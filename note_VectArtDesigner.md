# VectArtDesigner 実装ノート

## 現在の位置付け

`VectArtDesigner`はIBM WebArt Designerとの互換性を目標に、MIFを主保存形式とする単体アプリである。
互換性はMIFコンテナーだけでなく、WebArt Designerのオブジェクト構造、編集操作、プロパティ設定と
保存結果の対応を基準にする。SVGも読み書きするが、編集モデルとUIはMIFで表現できる範囲へ限定する。
編集モード切替はなく、常に同じMIF仕様で編集する。

WebArt Designerの全機能をそのまま複製することは目的にしない。当面は作図・編集ツールとしての
機能追加を優先し、各ツールの実装時に実際の操作と操作前後のMIF差分を調査する。調査結果から
互換対象、変換対象、本アプリには不要な機能を分類する。

## プロジェクト構成

- 実行プロジェクト: `VectArtDesigner.dpr` / `VectArtDesigner.dproj`
- コアモデル: `Source/Core`
- 編集操作: `Source/Editor`
- 描画: `Source/Rendering`
- MIF永続化: `Source/Persistence/Mif`
- SVG永続化: `Source/Persistence/Svg`
- シェルとメニュー: `Source/Shell`
- レイヤーUI: `Source/Layers`
- オブジェクト設定: `Source/ObjectProperties`
- AviUtl2プラグインプロジェクトと専用ライブラリは2026-08-31の方針変更で削除した。
- 類似機能の実装時は`D:\DelphiProg\test\SYNC_ScreenLayout`のツール、モデル、作成・操作処理、描画、
  プロパティUI、Undo／Redo、テストを都度参照する。AviUtl2連携、プラグイン用公開口、独自JSON仕様は
  移植せず、本プロジェクトの単体アプリ方針とWebArt Designer互換MIFモデルへ適合させる。

## 編集キャンバスの描画経路と性能

- 編集キャンバスの最終合成、背景、選択表示は、利用可能な環境ではVCLの`TDirect2DCanvas`を使う。
  Direct2Dが利用できない場合または描画例外時はGDIへフォールバックする。
- Documentの図形本体はDirect2Dベクター描画ではない。共通レンダラーがSkia CPU raster-directで
  キャンバス全体を透明RGBA8へ描き、premultiplied BGRAの`TBitmap`へ変換した後でDirect2Dへ渡す。
- Document Revisionが変わるたび図形本体を全体再描画するため、大きなキャンバスや多数レイヤーでは
  頂点移動中の主な負荷になる。縮小表示時は実際の表示寸法までで描き、同一寸法の描画バッファを再利用する。
- 移動、リサイズ、回転、Path頂点移動は`BeginInteractiveUpdate`から`EndInteractiveUpdate`までの
  対話更新として扱う。ドラッグ中はキャンバスだけをライブ更新し、その他のUIは終了時に同期する。
- さらに負荷が問題になる段階では、変更図形の旧・新Boundsを使った部分再描画、または編集中レイヤーを
  背景キャッシュから分離する方式を優先して検討する。
- ベジェの制御点計算と表示用分割は`VectArtDesignerBezierGeometry`、手書き入力の間隔判定と頂点整理は
  `VectArtDesignerFreehandGeometry`へ分離し、図形作成の入力状態から幾何アルゴリズムを独立させる。

## 実装済みモデル

- レイヤーは実行中に不変の`LayerId`、フラットな`GroupId`、描画内容ごとの`Revision`を持つ。
  DocumentはIDから現在位置、GroupIdから積層順の所属レイヤー列を引く関係キャッシュを遅延構築する。
  グループの入れ子構造は採用しない。`Ctrl+G`／`Ctrl+Shift+G`と右クリックメニューでグループ化・解除し、
  1メンバーの選択を全メンバーへ展開する。混在型グループの移動・リサイズとUndo／Redoに対応する。
  右クリックの`グループ`サブメニューは`グループ化`と`グループ解除`のみとし、専用の再グループ化は
  Undoと重複するため提供しない。同一グループへ再度グループ化する操作も無効にする。
  レイヤー一覧はグループを1行へ集約し、グループ行のダブルクリックで所属レイヤー行を見出しの下方向へ
  展開／折り畳みする。展開時はグループ見出しと全メンバーの外側を共通枠で囲み、所属範囲を明示する。
  グループ行には閉じた状態で`＋`、開いた状態で`－`のボタンを常時表示し、シングルクリックでも開閉する。
  折り畳み行は全メンバーを元の配置・重なり順で合成したサムネイルを表示する。展開状態、表示行対応、
  GroupIdと各メンバーRevisionを基準にしたサムネイルは一覧側のキャッシュであり、Documentのフラットな
  所属関係は変更しない。
  サムネイル背景は白を既定とし、一覧の`ThumbnailBackground`設定で市松模様へ戻せる。
  MIF／SVGへのグループ所属保存は未接続である。
- Canvas: サイズ、背景色、透明背景
- Rectangle／Ellipse: 種別、座標、回転、塗り有無と色、枠線色、線幅、線種、表示、ロック、不透明度
- Line: 始点／終点、線色、線幅、線種、cap、join、アンチエイリアス、始点／終点マーカーとサイズ
- Path: 頂点列、開閉、塗り、輪郭色、線幅、線種、cap、join、アンチエイリアス、
  開いたPathの始点／終点マーカーとサイズ
- Image: 埋め込みPNG、取込元フルパス、4頂点配置、image／logo区分、表示、ロック、不透明度
- Text: 複数行文字列、横書き／縦書き、外接矩形、フォント名・サイズ・スタイル、字間率・行間率、文字色、回転、字形の左右／上下反転、表示、ロック、不透明度
- 内部データの線種、品質、マーカー関連は通常名を使用する。`Mif`名はコンテナーと形式変換処理に限定する。

## 編集UI

- アプリ全体へDelphi 37.0標準の`Windows Modern Dark` VCL Styleを適用する。メニューなどは
  標準VCLを優先し、正しくダーク化できない箇所だけ独自方式で補う。
- スクロールバーとトラックスライダーは独自UIを維持する。キャンバス、ツールアイコン、線種など
  内容自体の可視化が必要なコントロールは、ダーク化だけを目的とする独自実装とは区別する。
- `編集`メニューはUndo、Redo、キャンバス設定を提供する。モード項目は持たない。
- Rectangle、Line、Pathの生成既定値は`TVectArtEditorState`で共有する。
- ベジェ曲線ツールはクリックしたアンカー列から滑らかな3次ベジェを自動生成し、ダブルクリックまたは
  右クリックで確定する。Pathモデルには指定したアンカーとベジェ属性だけを保持し、描画と当たり判定時に
  3次ベジェを生成するため、編集頂点には指定アンカーだけを表示する。
- Document変更はUndo／Redoコマンドを通して適用する。
- 複数選択時は共通値だけを表示し、ロックされたレイヤーを含む場合は変更を禁止する。
- 選択ツールでオブジェクトを右クリックするとコンテキストメニューを表示する。右ドラッグは従来どおり
  キャンバス移動に使い、移動量が小さい右クリックだけをメニュー操作と判定する。反転は単一選択では
  自身の中心、複数選択では選択全体の外接範囲中心を共通軸にし、上下反転／左右反転を1件のUndo対象にする。
  文字は字形反転状態を保持し、画像、Line、Pathは配置点を反転する。ロックを含む選択では実行しない。
  作図ツール使用中の右クリックは選択ツールへ切り替え、カーソル直下にオブジェクトがなくても現在選択が
  あればその選択用メニューを表示する。Path／Bezier作成中は右クリックで図形を確定して選択ツールへ移り、
  同じ右クリックのマウスアップで確定直後のオブジェクト用メニューを表示する。
- 同じ右クリックメニューの回転には、左へ90度、右へ90度、180度を設ける。単一／複数選択の中心軸、
  対応オブジェクト、ロック判定、1件のUndo／Redoは反転と共通にする。任意の角度指定項目は追加せず、
  角度の数値操作は設定状態表示側で扱う。
- 同じ右クリックメニューの「非表示」はレイヤー一覧の表示ボタンと同じ`Visible`状態へ接続する。
  選択中の全オブジェクトが非表示ならチェックを付け、クリック時は複数選択をまとめて非表示／再表示し、
  1件のUndo／Redoとして扱う。非表示後も選択は保持し、オブジェクト本体は描かず選択枠と操作ハンドルを
  表示するため、空白上の右クリックから再表示や変形ができる。このオブジェクトメニューはキャンバスと
  レイヤー一覧で共通化し、一覧では選択済み行の複数選択を維持しながら右クリックで表示する。
- WebArt Designerの右クリックメニュー見本にある未実装候補は、オブジェクトの編集、切り取り、コピー、
  貼り付け、オブジェクト属性の貼り付け、削除、複製、ロールオーバーへ登録、非表示、重なり順、
  整列、イメージに変換、複製してイメージに変換、余白を取り除く、グループ、頂点の編集、曲線を閉じる、
  オブジェクトを送る、オブジェクトの保存、Web用保存ウィザードである。各機能を追加する時に、見本の
  階層と名称、既存の編集モデル、MIF往復結果を照合し、本アプリで必要なものだけを順次実装する。
  このうち回転の固定3項目は実装済みで、見本の「角度指定」は対象外とする。
- 編集キャンバスへの画像ファイルドロップは、Windows Imaging Componentで入力をPNGへ変換して
  Imageレイヤーへ埋め込む。取込元のフルパスは出典情報として保持するが、表示と編集では参照せず、
  元ファイルが移動・削除された後も埋め込みPNGを正本として扱う。複数ファイルは1回のUndo対象にする。
  画像のコピー＆ペーストは後続作業とする。
- 縮小表示時だけ線幅を画面上1px以上へ補正し、DocumentおよびMIF／SVG出力値は変更しない。
- 開いたPathは始点／終点マーカーと各サイズを編集できる。閉じたPathではマーカーを描画せず、
  Object Propertiesの対応項目を無効化する。
- Lineと開いたPathのマーカーはレイヤー一覧のGDI／Direct2Dサムネイルにも表示し、マーカー部分の
  クリックで対象レイヤーを選択できる。Path選択枠は頂点範囲を編集基準として維持する。
- 手書きはドラッグ軌跡をマウスアップ時に鋭角頂点の連続直線Pathへ変換する方式と、連続ベジェPathへ
  変換する方式の両方を実装している。入力中は軌跡を折れ線表示し、確定時に頂点整理と曲線変換を行う。
- パレットは選択、直線、連続線グループ、手書きグループ、Ellipse、Rectangle、Rounded Rectangle、
  Closed Path、Closed Bezier、Textの10ボタンとする。連続線と手書きの
  各グループは、選択中のボタンを再クリックすると直線版／ベジェ版を交互に切り替え、アイコンにも反映する。
- Rectangleボタンは枠のみ、塗りのみ、枠＋塗りの3モードを現在のアイコンへ反映し、選択中の再クリックで
  循環する。`R`も初回はRectangleを選択し、再入力すると同じ順でモードを切り替える。
- Ellipseボタンも同じ3モードを持ち、`E`と選択中の再クリックで循環する。ドラッグ中のShiftは
  Rectangleと同じ縦横比1:1の制約として扱い、正円を作成する。
- Rounded Rectangleボタンも同じ3モードを持ち、仮キー`U`と選択中の再クリックで循環する。
  横方向の丸み:中央:丸みを作成時1:8:1とし、高さの半分で制限する。輪郭は各1/4円弧を6分割した
  閉じたPathとして確定する。頂点編集は無効にし、四角と同じ外接枠で移動・リサイズするため、
  作成後のリサイズでは丸みを再計算せず図形全体を変形する。
- Closed Pathボタンも同じ3モードを持ち、`C`と選択中の再クリックで循環する。頂点を順に指定し、
  始点クリック、ダブルクリック、右クリックのいずれでも鋭角の閉じたPathとして確定する。
- Closed Bezierボタンは独立した同じ3モードを持ち、仮キー`G`と選択中の再クリックで循環する。
  アンカー指定から滑らかな閉ベジェPathを直接作成する。
- 編集キャンバス上のツールキーは選択`S`、直線`L`、連続線グループ`P`、手書きグループ`B`、
  Ellipseグループ`E`、Rectangleグループ`R`、Rounded Rectangleグループ`U`、Closed Pathグループ`C`、
  Closed Bezierグループ`G`とする。
  Textは`T`で選択し、キャンバスのクリック位置から入力を開始する。既存Textのクリックでは最寄りの
  文字間へキャレットを置く。Enterは明示改行、Escapeはキャンセル、外側クリックは確定として扱い、
  Windows IMEの未確定文字列と確定文字列を分離して受け取る。
  Textの外接矩形は、最も幅の広い行を横の基準、行数を縦の基準とし、文字ごとの幅、字間率、
  フォント行高、行間率から基準レイアウトを求める。選択枠のリサイズ後は、基準レイアウトから外接矩形への
  X/Y倍率で全グリフと間隔を変形する。Object Propertiesの字間(%)・行間(%)変更時は現在のX/Y倍率を保つ。
  Vertical writingを有効にすると、明示改行で分けた行を右から左の列へ組み直す。字間率は列内の上下間隔、
  行間率は列同士の左右間隔へ適用する。選択フォントに対応する`@`付き縦書きフォントが存在する場合は
  描画時に自動利用し、Document上の指定フォント名は維持する。
  `P`と`B`の再入力はボタン再クリックと同じ切替を行う。文字入力欄では発動させず、`B`が将来の機能と
  競合した場合だけ割当を変更する。
- 派生図形は専用レイヤー型ではなく、閉じたPathの頂点データを生成するプリセットとして扱う。
  基本作図ツールより優先度を下げる。

## MIFとSVG

- MIF読込では既知のRectangle、Ellipse、Line、Path、Image、TextをDocumentへ変換する。
- WebArtの`object type=logo`は`logo text unicode`のUTF-16LE文字列、フォント属性、色、4点配置を
  Textへ復元する。`logo writing mode`は横書き／縦書きフラグとして扱い、新規TextもPNG表示本体と
  同じメタデータを持つ`logo`として書き出す。反転文字は4点配置の向きへ反映し、再読込時は負の向きを
  字形反転として復元する。
- ベジェ生成結果は現時点ではMIFへ連続直線として保存される。WebArt Designer固有の曲線コマンドは
  コマンド4が3次ベジェ区間であることを確認した。閉じた曲線も`vector closed=1`で保存できるが、
  元アプリの閉鎖操作は始点と終点を直線で結ぶ。type 9/11を含むネイティブ読書きへ置き換えるまでは、
  現行どおり16分割の直線列への変換として明示する。
- MIF保存では元コンテナーを可能な範囲で再利用し、変換や非対応内容を`TMifExportReport`で返す。
- MIF生成を伴わない`TryAnalyzeVectArtMifExport`を編集状態の判定に使い、ステータスバーへ
  完全互換、変換あり、非対応を表示する。詳細はステータスのヒントへ表示する。
- SVGはMIF編集モデルへ変換できる要素と属性だけをDocumentへ取り込む。
- 詳細な対応範囲と変換・無視・エラー方針は[SVG互換表](svg_compatibility.md)を参照する。
- SVG読込で変換または無視した描画要素と装飾は`TSvgImportReport`へ記録し、アプリ上で通知する。
- SVGルートの`viewBox`と`preserveAspectRatio`はキャンバス座標へ変換する。角丸、個別不透明度、
  fill-rule、線の補助属性、filter／clip／mask／合成指定などMIFへ保持できない属性も読込レポートへ記録する。
- SVG保存はDocumentの値を標準SVG属性へ出力し、Textは`text`と行ごとの`tspan`へ保存する。
  線種・マーカー、文字外接寸法、字間率・行間率、画像取込元パスなど必要な補助情報は
  既存のVAD名前空間で保持する。
- MIFとSVGのどちらから開いても同じDocument型と同じ編集UIを使用する。

## 現在の検証対象

- Document選択・Revision
- Rectangle、Line、Path、Image、Textの操作
- Object PropertiesとLine Toolbar
- 共通レンダラー
- Document JSON往復
- SVG往復と外部SVG取込
- MIFコンテナー往復、Document往復、WebArtサンプル取込

## ビルド

Debug:

```bat
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\VectArtDesigner\VectArtDesigner.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

Release:

```bat
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\VectArtDesigner\VectArtDesigner.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

## スナップ操作（2026-09-08）

- `D:\DelphiProg\test\SYNC_ScreenLayout`の`ScreenLayoutSnapGeometry`と入力処理を参照して、
  `Source/Editor/Geometry/VectArtDesignerSnapGeometry.pas`へ候補探索を移植した。
  キャンバス座標は左上原点へ合わせ、グループは本アプリのフラットな選択・所属関係を使用する。
- 通常移動（複数選択・グループを含む）、回転していない図形のリサイズ、Line端点、Path頂点、
  直線・連続線・基本図形の作成に適用する。移動は選択全体の辺・中心を基準に相対配置を維持する。
- 候補はキャンバスと表示中の他図形の辺・中心、編集中Pathの別頂点との水平・垂直一致とする。
  選択中レイヤーは移動・リサイズの候補から除外する。候補がない軸はズーム連動の不可視グリッドへ吸着する。
  吸着距離は画面上6px、グリッド間隔は画面上約48pxを目安に1・2・5系列で切り替える。
- Altでスナップを一時解除する。矩形系作成では従来のAlt中心基準作成も維持する。
  直線・連続線作成のShiftは水平・垂直拘束、回転のShiftは15度刻みとする。
  通常の回転と直線・連続線作成は30度系・45度系へ5度以内で近接吸着する。
- 吸着線と対象外接枠をDirect2D／GDIで点線表示し、回転吸着中は回転ハンドル色を変更する。
  操作終了・キャンセル・Alt解除時にガイドを消す。手書きの軌跡は吸着で変形させない。
- 回転図形のリサイズと画像単体の四隅リサイズの吸着は未対応。正方形・中心基準の作成では
  最終寸法補正を優先し、補正前の候補ガイドを残さない。他図形の個別頂点・平行・直交は後続候補とする。
- MIF／SVG／Documentへスナップ固有情報は保存しない。確定した座標と角度を既存履歴・保存処理へ渡す。

## 追加予定：色・塗り設定UI

2026-09-08に元の色・塗り設定UI方針書の内容を統合した。以下は今後の実装方針であり、実装済み機能ではない。

### 共通ポップアップとツール側表示

- 大きな固定カラーピッカーを画面へ常設しない。各ツールには現在状態の小型プレビューのみを置く。
  ベタは単色スウォッチ、グラデーションは縮小表示、テクスチャは縮小サムネイルで示す。
- プレビューをクリックすると、クリックした項目を対象に共通の色・塗り編集ポップアップを開く。
  基本1つの非モーダルなトップレベルフォーム等を使用し、複数のカラーピッカーは同時表示しない。
  編集結果はキャンバスへ即時反映する。呼び出し元に応じて表示内容を切り替える。
- 図形・文字・影・枠の各ツールへ複雑な色編集UIを分散させず、色指定箇所や塗り方式の増加は
  プレビューの追加と共通ポップアップの拡張で対応する。色専用に広い常設領域を確保しない。

### 属性ごとの編集内容

- 影、単色しか持たない文字装飾などのベタ専用要素では、塗り方式の選択を表示せず、
  単色カラーピッカーと使用された色一覧を表示する。
- 図形の塗り・枠や複数方式に対応する要素では、ベタ／グラデーション／テクスチャの切替、
  最近使った塗り、選択中方式の編集UIを表示する。影は原則ベタのみとする。
- 文字は文字色に加え、文字固有の装飾によって1～2箇所程度の色指定が増える場合を想定する。
  それぞれに現在色または現在塗りのプレビューを置き、クリックされた項目だけを編集する。
  1つの固定カラーピッカーで文字全体を編集する方式にはしない。
- テクスチャ編集はテクスチャ選択、プレビュー、必要に応じた関連設定を表示する。

### グラデーション編集

- 色1と色2を別々のポップアップで編集せず、1つのポップアップ内で完結させる。
- グラデーションのプレビュー、種類、色1・色2の選択、共通カラーピッカー、使用された色一覧を配置する。
  カラーピッカー自体は1つだけとし、色1・色2の選択で編集対象を切り替える。
- 将来3色以上へ拡張する場合も、対象色を選び共通カラーピッカーで編集する考え方を維持する。

### 塗り全体と単色の再利用

- 上位の「最近使った塗り」は完成した塗り設定一式を再利用する。ベタ、グラデーション、テクスチャの
  プレビューを並べ、クリックで現在の対象へ一式を適用する。
  グラデーションは種類・色1・色2・その他必要な設定を含め、テクスチャは使用テクスチャ情報を含める。
  図形Aの完成した見た目を図形Bへそのまま適用する用途とする。
- 下位の「使用された色」は単色だけを色チップとしてカラーピッカー内へ並べる。
  過去に使用した色、現在のデザイン内の色、再利用可能な色を扱い、グラデーションやテクスチャは含めない。
  UIで「履歴」という表現を前面に出す必要はない。
- 既存の図形で使用した色A・色Bを、新しいグラデーションの色1・色2へクリックで設定できるようにする。
  塗り一式の再利用と構成色だけの再利用を分け、デザイン内の色統一にも利用する。

## 次の作業

1. レイヤー単位の`Revision`を描画キャッシュへ接続し、1レイヤー編集時の再描画範囲を限定する。
2. WebArt DesignerのグループMIF構造を調査し、アプリ固有情報なしでグループ所属を保存・復元する。
3. 曲線MIFのコマンド4とtype 9/11をネイティブに読み書きし、閉じたベジェの曲線編集情報を維持する。
4. 角丸四角の元アプリ製MIFサンプルが揃ったら、現在の閉じたPath表現と専用element type、
   再読込時の図形種別を比較する。
5. 文字の縁取り、影・炎などの効果と縦書きは、追加MIF差分を確認して互換・変換・対象外を分類する。
6. 各ツールについてUI設定、MIFメタデータ、再読込結果の対応表を作る。
7. 調査した機能を互換対象、変換対象、不要機能に分類する。
8. 派生図形を閉じたPathの低優先度プリセットとして追加する。
9. MIF互換性詳細を常時開く専用UIが必要か、実際の編集操作で確認する。
10. 単体アプリ化後も残る外部ホスト用APIとコメントを整理する。
11. 色・塗り設定UIを小型プレビューと共通非モーダルポップアップへ統一し、塗り一式と単色の再利用を実装する。
