# VectArtDesigner / WebArt Designer系 MIF 仕様メモ
## グラデーション・テクスチャ

作成日: 2026-09-08  
対象: `四角_グラデーション*.mif` / `四角_テクスチャ.mif` の再解析結果を整理した暫定仕様メモ

---

## 1. 概要

今回の再解析から、MIF 内の塗り系表現は **`waDAtexture` 系の共通構造** で保存され、
実際の見た目の違いは **`waDAtexture object type`** によって切り替えられている可能性が高い。

少なくとも今回確認できた種別は以下の通り。

- `gradation linear`
- `gradation circle`
- `gradation square`
- `gradation radiate`
- `gradation wave`
- `spectrum linear`
- `image`

また、同系統の構造として `color` も確認できるため、塗り全体を共通の texture 系オブジェクトで扱っていると考えられる。

---

## 2. 共通して見つかった主なキー

グラデーション・テクスチャ関連では、以下のキーが繰り返し現れた。

| キー名 | 内容 | 備考 |
|---|---|---|
| `waDAtexture object type` | 種別 | 最重要。描画方式の切り替えに使用 |
| `waDAtexture color1` | 色1 | 多くの種別で使用 |
| `waDAtexture color2` | 色2 | 2色系グラデーションで使用 |
| `waDAtexture angle` | 角度 | 線形系・スペクトル系で有効と考えられる |
| `waDAtexture level` | レベル/数 | 波状グラデーションで有効と考えられる |
| `waDAtexture pathname` | 付随データ | 画像テクスチャでは元画像パス。その他の種別では意味未確定 |

### 2.1 現時点の読み取り方

- `object type` が「何を描くか」を決める本体。
- `color1`, `color2`, `angle`, `level`, `pathname` は **共通枠として常に存在しやすい**。
- ただし、種別によって **実際に使う項目と使わない項目が異なる**。

---

## 3. 種別ごとの観測結果

## 3.1 線形グラデーション

- 種別: `gradation linear`
- 観測例:
  - 黄 → 橙の線形グラデーション
  - 赤 → 青、角度変更版

### 観測できた特徴

- `color1`, `color2` が使用される。
- `angle` を変えることでグラデーション方向が変化する。
- 2色の直線的な遷移として表示される。

### 暫定解釈

線形グラデーションは、2色と角度で定義される基本的なグラデーション。

---

## 3.2 円形グラデーション

- 種別: `gradation circle`
- 観測例: 赤と青による円形の遷移

### 観測できた特徴

- `color1`, `color2` が使用される。
- `angle` は構造上保存されるが、見た目への影響は今回の範囲では未確認。
- 中心基準の円形変化ではなく、画面上では左右方向の寄りを持つ円形分布に見える。

### 暫定解釈

名称は circle だが、単純な同心円塗りだけではなく、内部実装上の中心位置や方向の概念が別にある可能性がある。
ただし今回の検証では、その位置情報までは切り分けできていない。

---

## 3.3 角形グラデーション

- 種別: `gradation square`
- 観測例: 中央赤、外周青の角形遷移

### 観測できた特徴

- `color1`, `color2` を使用する。
- 見た目は中心から外周へ向かう **四角ベース** のグラデーション。
- `angle` は保存されるが、今回の観測では意味未確定。

### 暫定解釈

矩形の等高線のように色が広がるタイプ。
円形ではなく、四角い輪郭で色が変化する。

---

## 3.4 放射状グラデーション

- 種別: `gradation radiate`
- 観測例: 中央赤、外周青の滑らかな放射状変化

### 観測できた特徴

- `color1`, `color2` を使用する。
- 見た目は一般的な radial gradient に近い。
- `angle` は保存されるが、今回の範囲では未使用または影響が小さい可能性がある。

### 暫定解釈

円形グラデーションとは別実装で、こちらがより一般的な「中心から外へ広がる放射状」表現である可能性が高い。

---

## 3.5 波状グラデーション

- 種別: `gradation wave`
- 観測例: 赤・青が同心円状に交互に並ぶ波紋パターン
- UI 上の設定例: `数(M) = 4`

### 観測できた特徴

- `color1`, `color2` を使用する。
- `level` が `4` になっている例を確認できた。
- 見た目は色の単純補間ではなく、**繰り返し縞** のような波状パターン。

### 暫定解釈

`level` は波の本数、密度、周波数、段数などに相当する可能性が高い。
少なくとも波状グラデーションでは、`level` が実際に意味を持つと考えてよい。

---

## 3.6 線形スペクトル

- 種別: `spectrum linear`
- 観測例: 角度 45° の虹色グラデーション

### 観測できた特徴

- 見た目は固定的な多色スペクトル（虹色）。
- `angle` により方向が変わる。
- `color1` / `color2` の項目自体は存在するが、通常の2色グラデーションのような意味では使われていない可能性が高い。

### 暫定解釈

`spectrum linear` は色1・色2で遷移を作るものではなく、
**既定のスペクトル配列を角度付きで配置する専用種別** と考えるのが自然。

---

## 3.7 テクスチャ画像

- 種別: `image`
- 観測例: 外部 PNG を貼ったテクスチャ

### 観測できた特徴

- `waDAtexture object type = image`
- `waDAtexture pathname` に、元画像のフルパスが文字列として保存される。
- ファイル全体サイズが大きく、MIF 内部にも画像データ相当の大きなブロックが含まれている。
- `color1`, `color2`, `angle`, `level` は構造上存在するが、今回の例ではいずれも 0 系の値で、画像テクスチャとしては未使用と思われる。

### 暫定解釈

画像テクスチャは **外部画像参照情報 + MIF 内部データ** の両方を持っている可能性が高い。

### 現時点の結論

- `pathname` は **元画像ファイル情報** として保存される。
- ただし描画時に本当にその外部パスを参照しているのか、
  それとも MIF 内部に保持された画像データだけで再現できるのかは未確定。

### 今後の確認方法

以下を試すと確定しやすい。

1. 元画像ファイルを移動または削除する。
2. その状態で MIF を再読込する。
3. 正常表示されれば内部埋め込み優先、表示できなければ外部参照依存の可能性が高い。

---

## 4. 現時点で見えている構造モデル

今回の観測結果から、塗り情報は概ね次のようなイメージで整理できる。

```text
TextureObject
  object_type   : string
  color1        : color
  color2        : color
  angle         : number
  level         : number
  pathname      : string or binary-related field
```

そして実際の意味は `object_type` に応じて以下のように分岐する。

| object_type | 主に使う項目 | 役割 |
|---|---|---|
| `gradation linear` | `color1`, `color2`, `angle` | 2色線形 |
| `gradation circle` | `color1`, `color2` | 円形系 |
| `gradation square` | `color1`, `color2` | 角形系 |
| `gradation radiate` | `color1`, `color2` | 放射状 |
| `gradation wave` | `color1`, `color2`, `level` | 波状反復 |
| `spectrum linear` | `angle` | 虹色線形 |
| `image` | `pathname` | 画像テクスチャ |

---

## 5. 実装・移植時の扱い方の提案

VectArtDesigner 側で読む/保持する際は、少なくとも以下の設計にしておくと安全。

### 5.1 種別は列挙ではなく文字列互換を意識

`object type` は文字列で記録されているため、将来未確認種別が出ても保持できるよう、
内部列挙値に落とす前に **原文文字列も保持** したほうがよい。

### 5.2 共通構造 + 種別別解釈にする

個別構造に分けるより、

- 共通フィールドとして `color1`, `color2`, `angle`, `level`, `pathname`
- 種別ごとに「どれを使うか」を切り替える

という方式のほうが MIF 互換性を保ちやすい。

### 5.3 未確定値はそのまま保存する

とくに `pathname` と色値の厳密なバイト解釈は未確定部分があるため、
再保存互換を重視するなら **元の値を壊さず保持** する設計が望ましい。

---

## 6. 未確定事項

今回の再解析では、まだ以下は断定しないほうがよい。

1. `color1` / `color2` の厳密なバイト並び（RGB/BGR/Alpha 含む最終定義）
2. `pathname` がグラデーション種別にも存在する理由
3. `gradation circle` と `gradation radiate` の内部パラメータ差
4. `angle` が円形・角形・放射状で完全に無効なのか、別条件で効くのか
5. テクスチャ画像が外部参照必須か、内部埋め込みのみで完結するか

---

## 7. 暫定まとめ

- MIF のグラデーション・テクスチャは、**`waDAtexture` 系の共通構造** で管理されている可能性が高い。
- 実際の種類は **`waDAtexture object type`** で分岐する。
- 2色グラデーションでは `color1`, `color2` が中心。
- 線形系やスペクトル系では `angle` が重要。
- 波状グラデーションでは `level` が実際に意味を持つ。
- 画像テクスチャでは `pathname` が重要で、さらに MIF 内部にも画像データらしき内容を持つ。
- したがって VectArtDesigner 側は、**共通保持 + 種別別解釈** を基本とするのがよい。

---

## 8. 検証に使った主なサンプル

- `四角_グラデーション.mif`（線形）
- `四角_グラデーション(1).mif`（線形、色と角度変更）
- `四角_グラデーション(2).mif`（円形）
- `四角_グラデーション(3).mif`（角形）
- `四角_グラデーション(4).mif`（放射状）
- `四角_グラデーション(5).mif`（波状）
- `四角_グラデーション(6).mif`（線形スペクトル）
- `四角_テクスチャ.mif`（画像テクスチャ）

