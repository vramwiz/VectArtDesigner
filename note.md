# VectArt共通基盤 作業ノート

作業再開時に最初に確認する、3プロジェクト共通の設計方針と基本ルールを扱う。
単独編集アプリ固有の状況は [note_VectArtDesigner.md](note_VectArtDesigner.md)、画面レイアウト
プラグイン固有の状況は [note_ScreenLayoutPlugin.md](note_ScreenLayoutPlugin.md)、完了済み作業は
[HISTORY.md](HISTORY.md) に記録する。

## 全体構成

- 同じ編集UIと描画基盤を使用する3つの実行プロジェクトを、このリポジトリで管理する。
  - 単独配置編集アプリ`VectArtDesigner`。
  - AviUtl2上で配置データを表示・編集する`【画面レイアウト】`プラグイン。
  - グラフデータから専用レイヤーを生成し、表示・編集するグラフプラグイン。
- 3つの`.dpr`と`.dproj`はリポジトリルートへ配置する。
- プロジェクトから階層を上がる相対参照は使用せず、`Lib`と`Source`以下への下方向の参照だけを使用する。
- 共通コードをプロジェクト間でコピーしない。`Lib`内の同じPascalユニットとDFMを各プロジェクトから
  直接参照し、共通UIの変更が各プロジェクトの次回ビルド時に反映される構成とする。
- 当面は共通BPLや共通DLLを必須にせず、共有ソースを各EXE／プラグインへ静的リンクする。
- 各プロジェクト固有のホスト処理は`Source`以下で分離し、ビルド成果物の出力先もプロジェクト別に分ける。

## 共通UI・モデル

- 中央編集、レイヤー、編集ツール、オブジェクト設定を独立した`TFrame`として再利用する。
- アプリケーションやプラグインの外枠は、共通Frameの生成、接続、ホスト固有メニュー、ライフサイクルだけを扱う。
- デザイナー用Frameは`IVectArtDesignerContext`を共通の接続口とし、Document、編集履歴、EditorStateを
  個別の公開プロパティで受け取らない。
- Contextが参照するサービスの所有権と寿命はホスト側で管理する。
- AviUtl2から編集UIを開く場合、現在の映像を編集背景として供給する。背景は配置Documentや
  シリアライズデータへ含めず、編集時だけ使用する参照表示とする。
- 配置プラグインとグラフプラグインは共通のDocument、描画エンジン、シリアライズ、編集UIを使用する。
- グラフ固有機能は共通UIを複製せず、専用ツール、レイヤー、プロパティ画面を拡張インターフェースから追加する。
- レイヤー名を内部識別へ使用しない。レイヤー固有ID、グラフオブジェクトID、系列ID、データ項目ID、
  レイヤーの役割など、変更されない識別情報をモデル内へ保持する。

## 共通描画・アニメーション

- レイヤー合成、半透明表示、キャンバスの拡大縮小など負荷の高い編集表示にはDirect2Dを使用する。
- Direct2Dを初期化できない画面セッションでは、編集画面を失わないためGDI表示へフォールバックする。
- 大きな文字と装飾文字にはGoogle Skiaベースの`TextRenderer`を使用する。
- Google Skiaの共通文字描画は`Lib/TextRenderer`、Win64ランタイムは`Lib/Skia/Win64/sk4d.dll`へ配置する。
- アイコンボタンは画像リソースや`TImageList`へ埋め込まず、コードから描画する。
- 配置プラグインとグラフプラグインの最終表示は同じ描画エンジンを使用する。
- アニメーションは再生中の可変状態を積み上げず、Document、外部データ、AviUtl2の現在時刻、
  アニメーション定義から描画状態を決定的に評価する。
- シーク、逆再生、途中フレームからの書き出し、同一フレームの再評価で同じ描画結果になることを保証する。
- UI終了時にシリアライズ文字列と描画用の不変スナップショットを更新し、描画スレッドから編集中の
  VCL Controlや可変Documentを直接参照しない。

## 共通編集UI

- ダークモードを基本とし、暗色表示へ適合できない常設VCLコントロールは採用しない。
- 初期配置は上からメニュー、ショートカットバー、ワークスペース、下部ステータスバーとする。
- ワークスペースは左からレイヤー、細幅の編集ツール、中央編集領域、右側オブジェクト設定とする。
- レイヤーとオブジェクト設定は左右のドック領域へ配置し、中央編集領域が残りの幅を使用する。
- `表示`メニューの`レイアウト編集`で、中央編集領域を除くツールFrameの移動チップを表示する。
- ツールFrameはフローティングウィンドウへ分離でき、左右どちらのドック領域へも再結合できる。
- 分離時は元の配置スロットを削除して空パネルを残さず、ドック側の複数ツールは横方向の列として配置する。
- ドロップしたX座標から同じ側の挿入順を決める。
- レイアウト編集モード終了時は分離中のツールを最後の所属側へ戻し、結合済みの左右位置と順序は維持する。
- 現段階では既定状態へのリセット、タブ結合、上下分割は扱わない。
- Layers、Tools、Object Propertiesの表示・非表示を切り替え、再表示時は最後のドック側と順序へ戻す。

## 永続化境界

- 永続化処理はReaderとWriterのインターフェースを公開境界とし、画面やDocumentから具象の
  ファイル形式実装を直接呼ばない。
- Streamとファイルの入口を分け、引数と戻り値の所有権を公開コメントで明示する。
- 例外をアプリケーション境界の外へ漏らさず、破損データや未知形式でアプリを停止させない。
- 読み込み元の未知項目を安易に破棄せず、保持または安全に無視する方針を形式ごとに明確にする。
- 推測した仕様と実データで確認済みの仕様を区別して記録する。
- AviUtl2プラグインとの受け渡しは、Documentをシリアライズした1つの文字列値を基本とする。
- 描画時は毎フレーム文字列を解析せず、文字列変更時に内部モデルと描画用スナップショットを更新する。

## コード構成

- `Lib/Designer`以下を`Core`、`UI`、`Render`、`Animation`、`Serialization`、`Extensions`など、
  機能と責務ごとのフォルダーへ分ける。
- 1つのツールが独自のFrame、描画Control、モデル、設定画面を持つ場合は、同じツールフォルダー内で
  ユニットを分ける。
- 新機能を既存の大きなユニットへ追記する前に、独立した責務または拡張機能として分離できるか確認する。
- ホスト固有のファイルダイアログ、プラグインAPI、メインフォーム、設定値管理を共通UIへ持ち込まない。

## 共通ビルドルール

- Delphi 37.0を使用し、対象プラットフォームはWin64とする。
- DebugとReleaseの両構成を検証する。
- コンパイル警告とエラーを確認し、原則として警告0、エラー0で完了とする。
- `Win32`、`Win64`、`.dcu`、`.rsm`、`.exe`、`.dll`などのビルド成果物はGitへ追加しない。
- 日本語文字列リテラルを持つ`.pas`と`.dpr`はUTF-8 BOM付きで保存する。

## コメントルール

- コードを読めば分かる処理の言い換えではなく、目的、責務、注意点、状態や値の意味を書く。
- 古い仕様や現在の実装と食い違うコメントは、見つけた時点で更新する。
- 不要なコメント、同じ内容の重複、処理を日本語へ置き換えただけのコメントを増やさない。
- ユニット先頭には、そのユニットの目的と担当範囲を`//`で書く。
- `interface`の公開関数・手続きには、呼び出し側から見た責務、入出力、重要な副作用を書く。
- フィールドや定数の短い説明は行末へ置き、同じブロックでは`:`、`=`、`//`の位置を可能な範囲で揃える。
- レコードの各フィールドには用途または値の意味を書く。バイナリー形式に対応するレコードでは配置と型の理由も明記する。
- コメントと対象の宣言または実装の間に不要な空行を入れない。
- `var`ブロック内へローカル関数・手続きを置かず、補助処理は同じ`implementation`の独立関数へ分ける。
- `property`、`procedure`、`function`の宣言は、112文字以内なら折り返さない。

## 共通基盤の次の作業

1. 現在の共通候補ユニットと`VectArtDesigner`固有ユニットの依存関係を整理する。
2. ルートに3プロジェクトを置く前提で、`Lib/Designer`と`Source`以下の移動計画を作る。
3. 共通シリアライズ形式、安定したレイヤーID、描画用スナップショットの責務を設計する。
4. AviUtl2背景プレビューとグラフ固有UIを追加できる拡張インターフェースを設計する。
