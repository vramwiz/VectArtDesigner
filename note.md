# VectArtDesigner 作業ノート

作業再開時に最初に確認する、現在の開発状況と基本ルールだけを扱う短いメモ。
試行錯誤や完了済み作業をここへ蓄積せず、制作履歴は [HISTORY.md](HISTORY.md) に記録する。

## プロジェクト

- 製品名とDelphiプロジェクト名は `VectArtDesigner` とする。
- 作業フォルダーは `D:\DelphiProg\test\VectArtDesigner` とする。
- 現在は、IBM WebArt Designerの `.mif` データを調査し、互換読み書きの方針を検討している。
- MIFの調査結果と確度は [mif_analysis.md](mif_analysis.md) を参照する。

## 現在の段階

- `WRT2646\Client` のVCLプロジェクト設定を基に、Win64用のプロジェクト本体を作成済み。
- メインフォームへ4つのFrameを配置し、左右へ再構成できるドッキングUIを先行検証する。
- MIFのコンテナー、埋め込みPNG、主要な `waDA*` メタデータの構造を解析中。
- 実装開始前に、Reader/Writerの責務、未知データの保持方針、破損データ時の動作を決める。

## UI・描画方針

- 初期実装ではドッキング配置の確認を優先し、各領域はツール名が分かる表示だけを持つ。
- アプリケーションの外枠だけを `TMainForm` とし、中央編集、レイヤー、編集ツール、
  オブジェクト設定は独立した `TFrame` とする。
- 初期配置は、上からメニュー、ショートカットバー、ワークスペース、下部ステータスバーとする。
- ワークスペースは左からレイヤー、細幅の固定編集ツール、中央編集領域、右側オブジェクト設定とする。
- レイヤーとオブジェクト設定は左右のドック領域へ配置し、中央編集領域が残りの幅を使用する。
- ダークモードを基本とし、暗色表示へ適合できないVCLコントロールは採用しない。
- Windowsの非クライアント領域にはDWMのダークタイトルバー属性を適用する。
- アイコンボタンは後で `SYNC_Lyrics_ToolbarButtons.pas` をコピーして使用する。画像リソースや
  `TImageList`へ埋め込まず、コードから描画する。
- レイヤー合成、半透明表示、キャンバスの拡大縮小など負荷の高い編集表示にはDirect2Dを使用する。
  単純なレイヤー名一覧の表示だけをD2Dへ寄せる必要はない。
- 大きな文字と装飾文字には、`Syncroh2_Filter_SerifDraw`で使用しているGoogle Skiaベースの
  `TextRenderer`をコピーして使用し、`sk4d.dll`を実行ファイルと同じ場所へ配備する。
- 初期ドッキング構成の確認を完了し、Direct2DキャンバスとSkia文字描画基盤の実装へ移行する。
- 中央編集領域の論理キャンバスは当面1920×1080固定とし、暗い編集領域内へ全体が収まる倍率で
  中央表示する。100%未満で表示しても論理座標と出力サイズは1920×1080を維持する。
- キャンバス背景と白い編集面は`TDirect2DCanvas`で描画する。Direct2Dの初期化に失敗した
  画面セッションでは、編集画面を失わないためGDI表示へフォールバックする。
- Google Skiaの共通文字描画は`Lib/TextRenderer`、Win64ランタイムは
  `Lib/Skia/Win64/sk4d.dll`へ配置する。ビルド後にDLLを実行ファイルと同じ場所へコピーする。
- アプリケーション起動時にSkiaランタイムを取得し、終了時に解放する。現段階では文字オブジェクトを
  まだ配置せず、文字描画ライブラリを利用できる基盤だけを組み込む。
- `表示`メニューの`レイアウト編集`でレイアウト編集モードを切り替える。通常時はチェックなし、
  編集モード時はチェックありとする。
- レイアウト編集モードでは、中央編集領域を除くツールFrameの上部に移動チップを表示する。
- 移動チップをドラッグするとツールFrameを専用のフローティングウィンドウへ分離し、
  ドック領域へドラッグして離すと再結合する。
- 分離後は移動チップを非表示にし、フローティングウィンドウのタイトルバーをドラッグして
  移動と再ドッキングを行う。ドッキング後はレイアウト編集用の移動チップを再表示する。
- ツールを分離した時点で元の配置スロットを削除し、残ったドック領域と中央編集領域を即時に
  再配置する。分離後の空パネルは残さない。
- ドラッグ中はワークスペース左右端へドッキング候補を表示し、左右どちらのドック領域へも
  結合できるようにする。
- 同じ側には複数のツールを横方向の列として配置できる。ドロップしたX座標から挿入順を決める。
- レイアウト編集モードを終了した場合は、分離中のツールを最後に所属していた側へ戻し、
  すべての移動チップを隠す。すでに結合済みの左右位置と順序は維持する。
- 現段階では既定状態へのリセット、タブ結合、上下分割は扱わない。
- `表示`メニューにはLayers、Tools、Object Propertiesのチェック項目を置き、各ツールの
  表示・非表示を切り替える。初回起動時は3ツールとも既定位置へドッキング表示する。
- レイアウト編集中はツール上の右クリックでダーク表示の`閉じる`メニューを出し、閉じた
  ツールは空き領域を残さず非表示にする。再表示時は最後のドック側と順序へ戻す。

## 設定保存

- 設定ルートは`TPath.GetDocumentsPath`から取得した`VectArtDesigner`フォルダーとする。
- メインフォーム位置とツール配置は`Documents\VectArtDesigner\MainForm.ini`へ保存する。
- `MainForm.ini`には形式バージョン、メインフォームの通常時座標・サイズ・最大化状態、各ツールの
  表示状態、ドック側、順序、ドック／フローティング状態、フローティング座標・サイズを記録する。
- 起動時は保存座標を現在のモニター作業領域へ補正し、画面外へ取り残されたフォームを戻す。
- ツールの表示状態は画面レイアウトの一部なので`MainForm.ini`へ保存する。将来追加する各ツール
  固有の設定値は`Documents\VectArtDesigner\<ToolId>\Settings.ini`へ分離する。
- INIのセクション名には表示文字列ではなく、変更しない内部IDを使用する。

## 基本ルール

- 例外をアプリケーション境界の外へ漏らさず、破損ファイルや未知形式でアプリを停止させない。
- 読み込み元に未知の項目がある場合、安易に破棄せず、保持または安全に無視する方針を明確にする。
- 推測で実装した仕様と、実データで確認済みの仕様を区別して記録する。
- 現在の方針が変わったときだけこの文書を更新し、完了事項は `HISTORY.md` へ移す。

## コード構成

- コードの肥大化を避け、Document、編集画面、レイヤー、各ツール、レイアウト管理、
  アプリケーション外枠の責務を小さなユニットへ分離する。
- `Source`直下へ実装ユニットを増やさず、`Core`、`Editor`、`Layers`、`Layout`、
  `ObjectProperties`、`ToolPalette`、`Shell`など、機能またはツール単位のフォルダーへ配置する。
- 1つのツールが独自のFrame、描画Control、モデル、設定画面を持つ場合は、同じツールフォルダー内で
  さらにユニットを分ける。複数ツールから使う型だけを`Core`または`Layout`へ置く。
- メインフォームはツール内部の描画・入力処理を持たず、Documentの生成、Frameの接続、
  アプリケーション全体のメニューとライフサイクルだけを担当する。
- 新しい機能を既存の大きなユニットへ追記する前に、独立した責務として分離できるか確認する。

## ビルドルール

- Delphi 37.0を使用し、対象プラットフォームはWin64とする。
- DebugとReleaseの両構成を検証する。
- コンパイル警告とエラーを確認し、原則として警告0、エラー0で完了とする。
- `Win32`、`Win64`、`.dcu`、`.rsm`、`.exe`、`.dll` などのビルド成果物はGitへ追加しない。
- 調査用の `.mif` ファイルはGit同期対象とし、バイナリーファイルとして扱う。
- 日本語文字列リテラルを持つ `.pas` と `.dpr` はUTF-8 BOM付きで保存する。

ビルドコマンド:

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\VectArtDesigner\VectArtDesigner.dproj"" /t:Build /p:Config=Debug /p:Platform=Win64"
```

```powershell
cmd /c "call ""C:\Program Files (x86)\Embarcadero\Studio\37.0\bin\rsvars.bat"" && msbuild ""D:\DelphiProg\test\VectArtDesigner\VectArtDesigner.dproj"" /t:Build /p:Config=Release /p:Platform=Win64"
```

## コメントルール

- コードを読めば分かる処理の言い換えではなく、目的、責務、注意点、状態や値の意味を書く。
- 古い仕様や現在の実装と食い違うコメントは、見つけた時点で更新する。
- 不要なコメント、同じ内容の重複、処理を日本語へ置き換えただけのコメントを増やさない。
- ユニット先頭には、そのユニットの目的と担当範囲を `//` で書く。
- `interface` の公開関数・手続きには、呼び出し側から見た責務、入出力、重要な副作用を書く。
- フィールドや定数の短い説明は行末へ置き、同じブロックでは `:`、`=`、`//` の位置を可能な範囲で揃える。
- レコードの各フィールドには用途または値の意味を書く。バイナリー形式に対応するレコードでは、配置と型の理由も明記する。
- コメントと対象の宣言または実装の間に不要な空行を入れない。
- `var` ブロック内へローカル関数・手続きを置かず、補助処理は同じ `implementation` の独立関数へ分ける。
- `property`、`procedure`、`function` の宣言は、112文字以内なら折り返さない。

## 次の作業

1. Rectangleツールから作成前の塗り色と不透明度を設定できるUIを設計する。
2. キーボード移動の連続入力を必要に応じて履歴上でまとめる方法を検討する。
3. レイヤー名の直接編集と重複名を許可するかを設計する。
