# VectArtDesigner 作業ノート

作業再開時に最初に確認する現在方針を扱う。実装状況と次の作業は
[note_VectArtDesigner.md](note_VectArtDesigner.md)、完了済み作業は[HISTORY.md](HISTORY.md)へ記録する。

## 設定UIの現在地（2026-09-08）

設定UIを先に揃える初期実装を完了。既存モデルで扱える色・文字・線設定はUndo付きで動作する。
共通色ポップアップ、設定タブ、9カテゴリ30種のテンプレ図形を追加した。
グラデーション・テクスチャはプレビュー、影・縁取り・効果は適用／保存前の設定UIである。
詳細と未対応範囲は実装ノート冒頭の「設定UIの初期実装」を参照。以下の追加予定仕様は最終目標として保持する。

## 製品方針

- `VectArtDesigner`は単体で動作するIBM WebArt Designer互換アプリとする。
- 互換性の基準はMIFファイルを読み書きできることだけでなく、WebArt Designerのオブジェクト構造、
  編集操作、プロパティ設定と保存結果の対応を再現できることとする。
- 当面は作図・編集ツールとしての機能を優先して充実させる。各ツールの実装時にWebArt Designerの
  実際の操作と、操作前後に生成されるMIFの差分を調査する。
- WebArt Designerの全機能を複製するのではなく、調査結果から本アプリに不要な機能を明示して対象外とする。
- AviUtl2プラグイン、画面レイアウトプラグイン、グラフプラグインとの共有を前提にしない。
- MIFを主保存形式とし、名前を付けて保存する場合もMIFを既定にする。
- SVGの読込と保存は維持するが、Documentと編集UIが扱う表現はMIFへ変換できる範囲に限定する。
- SVG固有表現を内部モデルへ増やさず、MIFとSVGの往復で共通して保持できる値を正本とする。
- 標準モードとMIF互換モードの区別は設けない。編集体系は常にMIF仕様とする。
- 線種、品質、始点／終点マーカーなどは通常のDocumentデータであり、型・プロパティ・UI・Undo名へ
  `Mif`接頭辞を付けない。MIFコンテナーやMIF変換処理など、ファイル形式固有の名前には`Mif`を残す。

## モデルとUI

- 中央編集、レイヤー、編集ツール、オブジェクト設定を独立した`TFrame`として構成する。
- 各Frameは`IVectArtDesignerContext`を通じてDocument、編集履歴、EditorStateへ接続する。
- Canvasはレイヤー一覧へ表示せず、`編集`メニューの`キャンバスの設定`から変更する。
- ダークモードを基本とし、アイコンはコード描画する。
- レイヤー名を内部識別へ使用しない。変更されない識別情報が必要になった場合はモデルへ明示的に追加する。
- グループはレイヤーを子として所有する階層構造にせず、通常レイヤー配列を維持したまま共通の
  `GroupId`を持つフラットな所属関係として扱う。各レイヤーの不変`LayerId`、現在位置、グループ所属列は
  Document内部の関係キャッシュを正本から遅延再構築する。
- 描画負荷の削減はレイヤー単位の`Revision`を基準にし、1レイヤー編集時に無関係なレイヤーの
  描画キャッシュを失効させない。構造変更では関係・合成順だけを更新し、既存レイヤーの描画内容は維持する。
- `Ctrl+G`は現在選択を1個のフラットグループへ統合し、`Ctrl+Shift+G`は解除する。グループの
  1レイヤーを選ぶと全メンバーを選択し、移動・リサイズ・右クリック操作は一体へ適用する。
  既存グループと他レイヤーをまとめる場合も入れ子にせず、新しい共通`GroupId`への付け替えとして扱う。
  右クリックでは親項目`グループ`の下に`グループ化`と`グループ解除`だけを置く。解除直後の所属を
  復元する専用の`再グループ化`は設けずUndoを使用し、すでに同一グループの選択にはグループ化を許可しない。
- レイヤー一覧ではフラットグループを通常レイヤーとは別の1行に集約する。グループ行のダブルクリックで
  メンバー行を見出しの下方向へ展開／折り畳みし、展開中は見出しから最下部メンバーまでを外枠で囲む。
  グループ行には折り畳み時`＋`、展開時`－`のボタンを常時表示し、このボタンのシングルクリックでも
  開閉できる。表示状態はDocument階層ではなく一覧専用の表示行キャッシュへ保持する。
  折り畳み行のサムネイルには全メンバーを実配置と積層順のまま合成する。グループ行の表示・ロック操作は
  所属メンバー全体へ1件のUndo操作として適用する。
- レイヤー一覧のサムネイル背景は市松模様を既定とする。`ThumbnailBackground`へ`vltbWhite`を指定すれば
  白へ戻せるよう、通常行とグループ合成行で共通の切替口を維持する。
- Lineと開いたPathのマーカーはレイヤーサムネイルへ描画し、キャンバス上でもクリック選択の対象にする。
  Pathの選択枠は頂点編集とリサイズの基準なので、装飾マーカーの外形では拡張しない。
- WebArt Designerの2つの手書きツールは、どちらもドラッグ軌跡をペンを離した時点でPathへ変換する。
  一方は軌跡を鋭角頂点の連続直線へ、もう一方は連続ベジェ曲線へ変換するため、両方を実装対象とする。
- ツールパレットでは連続直線／連続ベジェを1ボタン、手書き直線／手書きベジェを1ボタンにまとめる。
  選択中のボタンまたは対応キーを再入力するとペア内を切り替える。キーは選択`S`、直線`L`、
  連続線`P`、手書き`B`を基本とし、`B`が将来の機能と競合した場合だけ割当を見直す。
- Rectangleボタンは枠のみ、塗りのみ、枠＋塗りの3状態を持ち、選択中の再クリックまたは`R`の
  再入力で切り替える。塗り有効状態はDocumentへ明示的に保持し、MIF／SVGで往復する。
- Ellipseボタンも同じ3状態を持ち、選択中の再クリックまたは`E`の再入力で切り替える。
  2点目の指定中にShiftを押すと幅と高さを同じにして正円を作成する。
- Rounded Rectangleボタンも同じ3状態を持ち、選択中の再クリックまたは仮キー`U`で切り替える。
  作成時は左右の丸みと中央直線を約1:8:1とし、高さの半分を上限にする。確定後は閉じたPathへ
  変換するが頂点ハンドルは表示せず、四角と同じ外接枠で移動・リサイズする。リサイズ時は半径を
  再計算せず輪郭全体を変形する。
- Closed Pathボタンも同じ3状態を持ち、選択中の再クリックまたは`C`の再入力で切り替える。
  頂点を順に指定し、始点クリック、ダブルクリック、右クリックで鋭角の閉じたPathとして確定する。
- Closed BezierボタンはClosed Pathとは独立し、同じ3状態を選択中の再クリックまたは仮キー`G`で
  切り替える。指定アンカーを滑らかな閉ベジェPathとして直接確定し、元アプリで必要だった
  「ベジェ曲線を引く」「曲線を閉じる」の2操作を1つにまとめる。
- 派生図形は専用のDocument型を増やさず、閉じたPathの頂点データをプリセットとして生成する。
  優先度は低く、基本作図ツールの後に扱う。

## 描画

- Documentの描画は`Source/Rendering/VectArtDesignerRenderer.pas`へ集約する。
- 編集背景、選択枠、操作ハンドルなどの補助表示にはDirect2Dを使用し、利用できない場合はGDIへ
  フォールバックする。
- 大きな文字と装飾文字には`Lib/TextRenderer`のGoogle Skiaベース描画を使用する。
- Win64ランタイムは`Lib/Skia/Win64/sk4d.dll`へ配置する。

## 永続化

- MIFの確認済み構造は[MIF解析メモ](mif_analysis.md)を正として参照する。
- Reader／Writer往復、バイト差分、構造表示には[MIF診断ツール](Tools/MifDiagnostics/README.md)を使用する。
- MIFは対応内容だけをDocumentへ取り込み、未知情報が保存時に失われる場合は書出しレポートで通知する。
- MIF書出しではアプリ固有情報を新規追加しない。既存ベクターペイロードなどは可能な範囲で再利用する。
- 互換性判定とコンテナー生成は同じ処理で行い、完全対応、変換あり、非対応を生成結果と同時に返す。
- `TryAnalyzeVectArtMifExport`でMIFを生成せず同じ互換性レポートを取得できるようにし、編集中の
  ステータス表示と保存確認で判定内容を共有する。
- SVGはMIF編集モデルの交換形式とし、Rectangle、Line、Path、Imageと対応する装飾だけを扱う。
- SVGの対応表は[SVG入出力のMIF編集互換範囲](svg_compatibility.md)を正とし、変換・無視した
  描画要素は`TSvgImportReport`で読込後に通知する。
- MIFコンテナー内の既存Document JSONキーは、過去データを読めるよう読み書き互換性を維持する。

## コードとビルド

- Delphi 37.0、Win64を使用し、DebugとReleaseを検証する。
- Windows専用VCLアプリとして`Windows Modern Dark` VCL Styleを基本にする。標準VCLで正しく
  ダーク化できないコントロールだけ独自描画またはStyle Hookで補う。
- メニュー、ダイアログ、一般的なボタンと入力部品は標準VCLを優先する。スクロールバーと
  トラックスライダーは操作・描画仕様を維持するため独自UIとする。
- コンパイル警告0、エラー0を原則とする。
- ビルド成果物はGitへ追加しない。
- テスト・診断用EXEと本体の検証用ビルドは、必ずプロジェクト直下の`TestOutput/`配下へ出力する。
  ルートや`Tests/`へテストEXEを生成せず、従来の`Win64/UiTests`や`Win64/Verify*`等も使用しない。
  Delphiの直接コンパイルでは`-E`でEXE出力先、`-N0`でDCU出力先を明示し、
  `TestOutput/bin`、`TestOutput/dcu`配下へ置く。本体の検証用ビルドは
  `TestOutput/AppDebug`／`TestOutput/AppRelease`へ出力する。
  ログ・画像・一時データも`TestOutput/`配下へまとめる。通常の本体EXE更新は従来どおり別途行う。
  2026-09-08：既存のテスト・検証用EXEの一括削除は実行環境のポリシーで拒否され、未完了。
- 日本語文字列リテラルを持つ`.pas`と`.dpr`はUTF-8 BOM付きで保存する。
- ファイル編集後は`git diff --check`を実行する。
- ユニット先頭には目的と担当範囲を記載し、コメントは処理の言い換えではなく意図や制約を書く。

## 実装時の参照先

- `D:\DelphiProg\test\SYNC_ScreenLayout`では類似するベクター編集機能を実装しているため、
  ツール、モデル、作成操作、キャンバス操作、描画、プロパティUI、Undo／Redo、テストを都度参照する。
- 特に楕円、角丸四角形、円弧、自由線、ベジェPath、文字、変形処理は同プロジェクトの実装を確認する。
- 参照先のAviUtl2連携、プラグイン用公開口、独自JSON仕様は持ち込まず、VectArtDesignerの
  単体アプリ方針とWebArt Designer互換MIFモデルへ合わせて実装する。

## スナップ操作（2026-09-08）

- `D:\DelphiProg\test\SYNC_ScreenLayout`の`ScreenLayoutSnapGeometry`と入力処理を参照して、
  `Source/Editor/Geometry/VectArtDesignerSnapGeometry.pas`へ候補探索を移植した。
  キャンバス座標は左上原点へ合わせ、グループは本アプリのフラットな選択・所属関係を使用する。
- 通常移動（複数選択・グループを含む）、回転していない図形のリサイズ、Line端点、Path頂点、
  直線・連続線・基本図形の作成に適用する。移動は選択全体の辺・中心を基準に相対配置を維持する。
- 候補はキャンバスと表示中の他図形の辺・中心、編集中Pathの別頂点との水平・垂直一致とする。
  選択中レイヤーは移動・リサイズの候補から除外する。候補がない軸はズーム連動の不可視グリッドへ吸着する。
  吸着距離は画面上6px、グリッド間隔は画面上約48pxを目安に1・2・5系列で切り替える。
- Altでスナップを一時解除する。矩形系作成では従来のAlt中心基準作成も維持する。
  直線・連続線作成のShiftは水平・垂直拘束、回転のShiftは15度刻みとする。
  通常の回転と直線・連続線作成は30度系・45度系へ5度以内で近接吸着する。
- 吸着線と対象外接枠をDirect2D／GDIで点線表示し、回転吸着中は回転ハンドル色を変更する。
  操作終了・キャンセル・Alt解除時にガイドを消す。手書きの軌跡は吸着で変形させない。
- 回転図形のリサイズと画像単体の四隅リサイズの吸着は未対応。正方形・中心基準の作成では
  最終寸法補正を優先し、補正前の候補ガイドを残さない。他図形の個別頂点・平行・直交は後続候補とする。
- MIF／SVG／Documentへスナップ固有情報は保存しない。確定した座標と角度を既存履歴・保存処理へ渡す。

## 追加予定：色・塗り設定UI

- 各ツールには現在の色・塗りの小型プレビューを表示し、クリックで共通の編集ポップアップを開く。
  大きな固定カラーピッカーは常設しない。ポップアップは基本1つの非モーダルフォームとし、
  編集結果をキャンバスへ即時反映する。
- 呼び出し元に応じて単色専用と複数塗り方式を切り替える。図形の塗り・枠はベタ／グラデーション／
  テクスチャ、影や単色専用装飾は原則ベタのみとする。文字色と各装飾色は個別プレビューから編集する。
- グラデーション編集は1つのポップアップで完結させ、色1・色2の選択を共通カラーピッカーへ接続する。
- 再利用は「最近使った塗り」（設定一式）と「使用された色」（単色チップ）の2階層に分ける。
- 未実装の追加予定として登録する。詳細仕様は
  [実装ノートの色・塗り設定UI](note_VectArtDesigner.md#追加予定色塗り設定ui)を参照する。

## 追加予定：テンプレ図形と初期検討の追加候補

- テンプレ図形は低優先度の閉じたPath生成機能とする。形状は正規化した頂点・制御点で保持し、
  配置範囲へ展開後は通常Pathとして編集する。専用Document型や専用編集処理は増やさない。
- 一覧は実パスを描画し、現在の枠色・塗り色・線幅と枠のみ／塗りのみ／枠＋塗りを反映する。
  形状データ自体には色を持たせず、一覧と配置結果で同じ閉じたPath設定を使う。
- 基本・矢印・吹き出し・星／強調・フロー・UI／資料・図解・記号・装飾の分類で整理する。
  独自テンプレ登録、お気に入り、最近使用、検索等は将来拡張とする。
- 初期検討の追加候補として、高度なPath編集とブーリアン、文字・塗り・影の装飾、画像編集、整列、
  スポイトと各種プリセット、コピー・貼り付け、自動保存・復旧、フォント対応、性能・診断を登録する。
  各機能はWebArt Designerの操作・MIF構造を調査し、互換・変換・対象外を判断する。
- 詳細と図形候補一覧は[note_VectArtDesigner.md](note_VectArtDesigner.md)の該当節を正とする。
  初期検討の原文は[HISTORY.md](HISTORY.md)へ保管した。旧AviUtl2連携、SVG主保存、
  MIF書出し軽視、MIF外の独自モデル拡大は廃止済みのため追加予定へ戻さない。

## 追加予定：図形・文字オブジェクト設定

- 図形は「線／線の色／塗り潰しの色／影／情報」、文字は「文字／色／縁取り／効果／情報」を
  設定項目の整理基準とする。従来画面の配置をそのまま複製せず、共通設定UIへ再構成する。
- 線幅・線種・接合・線端・始終点マーカー・枠と塗りの3状態・アンチエイリアス、
  影の有効状態・ぼかし・独立色・横縦位置、位置・寸法・縦横比保持・透明度を扱う。
- 文字は複数行、フォントと自動サイズ、割合指定の字間・行間、スタイル、行揃え、横書き／縦書きを扱う。
  縁取りは「なし／通常／封蝋／白抜き／囲み／反転」、効果は
  「なし／ぼかし／動き／影／切り抜き／エンボス／炎」を追加・調査対象とする。
- 色の小型プレビューと共通ポップアップへプリセット色・RGB・16進値等を接続する。
  図形と文字の位置・サイズ・透明度・影設定を共通化し、フィルター、文字配置・フィット・パス追従、
  影や縁取りの分離／図形化との統合も検討する。
- 詳細パラメータと未確定事項は[note_VectArtDesigner.md](note_VectArtDesigner.md)の該当節を正とする。
  本登録は実装済みを意味せず、現在のMIF互換方針に沿って順次調査・実装する。

## 現在の次作業

1. レイヤー単位の`Revision`を編集キャンバスの描画キャッシュへ接続し、1レイヤー編集時はその
   レイヤーだけを再描画して積層順に再合成する。
2. WebArt DesignerのグループMIF構造を調査し、アプリ固有キーを追加せずフラットグループを保存・復元する。
3. 閉じたベジェツールのMIFは現時点で閉じた連続直線へ変換されるため、コマンド4とtype 9/11の
   ネイティブ往復を実装して曲線編集情報を維持する。
4. 角丸四角は作成時1:8:1、確定後は閉じたPathとして実装済み。元アプリ製MIFサンプルが揃ったら、
   専用element typeの有無と再読込時の図形種別を比較する。
5. 文字の基本入力と`logo` MIF往復は実装済み。次は曲線系、文字装飾、塗り系をWebArt Designerの
   操作とMIF構造を確認しながら順に調査・追加する。
6. 各ツールについてUI設定、MIFメタデータ、再読込結果の対応表を作る。
7. 調査した機能を互換対象、変換対象、不要機能に分類する。
8. 派生図形を閉じたPathの低優先度プリセットとして追加する。
9. MIF互換性の詳細をステータスヒント以外から常時開けるUIが必要か確認する。
10. MIF特化後も残っている不要な外部ホスト向け公開口とコメントを整理する。
11. 色・塗り設定UIを小型プレビューと共通非モーダルポップアップへ統一し、塗り一式と単色の再利用を実装する。
12. 初期検討から引き継いだ追加候補をMIF互換性と必要性で評価し、対応範囲を順次確定する。
13. 図形・文字オブジェクト設定の項目表に沿って、影・縁取り・効果・文字配置等を調査し、共通設定UIへ統合する。

- 2026-09-08追記：閉じた図形のグラデーション／テクスチャはUI先行から実適用へ移行。線形グラデーションは提供MIFを基に保存・再読込まで対応。放射／画像テクスチャはSVG保存対応、MIF属性は未検証。最新範囲は実装ノート冒頭を参照。

- 2026-09-08整理：PNGメタデータ、SVG書出し、共通塗り描画、色見本を別ユニット化。色UIはSource/ObjectProperties/Colorへ分類。構成と検証結果は実装ノート末尾「区切りのユニット整理」を参照。

- 2026-09-08区切り：色UI・ピッカー・共通数値スライダー・角度矢印まで完成。プレビュー描画をPaintPreview、線形座標計算をGradientGeometryへ分離。今回の追加・変更ユニットのコメントを目的・意図・制約中心に整理。再開時は実装ノート末尾を参照。

- 2026-09-09：放射状グラデーションのMIF保存・再読込に対応。提供サンプルのgradation radiate属性を使用。画像テクスチャのMIF保存は未対応。検証範囲は実装ノート末尾を参照。

- 2026-09-09表示修正：放射状は元アプリに合わせ、実座標の円形・半径は対角線の半分へ変更。共通描画とSVGを修正し、元MIF画素による回帰テストを追加。

- 2026-09-09：円形グラデーション（gradation circle）を追加。偏角で2色を補間し、UI・MIF往復・Undoへ接続。SVGは塗りPNG＋再編集用2色。元MIFとの内側画素差は各成分最大1。

- 2026-09-09：角形グラデーション（gradation square）をUI・描画・MIF往復へ追加。元MIFとの内側画素差は各成分最大1。通常EXE更新は起動中のため未完了、更新版はTestOutput/AppDebug。

- 2026-09-09：波状グラデーション（gradation wave）と繰り返し数を追加。UI・描画・MIF往復・Undoへ接続。元MIFの繰り返し5との内側画素差は各成分最大1。通常EXE更新済み。

- 2026-09-09：線形スペクトル（spectrum linear）を追加。開始色・角度のUI、描画、MIF往復、Undoへ接続。SVGは多ストップの標準linearGradient。赤・45度の元MIFとの内側画素差は各成分最大1。通常EXE更新済み。

- 2026-09-09：線・枠へ全6種類のグラデーションを実装。線の色UI、モデル、描画、作成引継ぎ、MIF／SVG／JSON、Undoへ接続。各辺で繰り返さず図形全体の座標を共有する。元MIF6種との色領域差は各成分最大1（輪郭境界を除外）。通常EXE更新済み。

- 2026-09-09：文字色へ全6種類のグラデーションを追加。文字全体の色場を字形へ適用し、UI・Undo・MIF／SVG／JSON・サムネイルへ接続。元MIFとの色場差は各成分最大1（角形は最大2）。通常EXE更新済み。

- 2026-09-09：図形の塗り・線・文字のグラデーションをいったん完成とする。MIF読込／PNG描画／配置／ペイント、SVGペイント定義、手続きシェーダーを分割。描画と編集コマンドのフォルダを責務別に整理し、コメントを目的・意図・制約中心に更新。構成はSource/README.md。

- 2026-09-09：線・塗り色・文字色の画像テクスチャを共通UI、実寸描画、MIF／SVG／JSON、Undoへ接続。元MIF3種のimage属性と埋込PNGを使用。11件のテストとDebug／Release検証通過、通常EXE更新済み。配置の確認範囲と未確認事項は実装ノート末尾を参照。

- 2026-09-09区切り：ユーザー動作確認済みの画像テクスチャを完成とした。SVGペイント読込と画像PNG変換を別ユニットへ分離し、責務・互換制約コメントとSource/README.mdを更新。7件の回帰テスト、Debug／Release検証通過、通常EXE更新済み。

- 2026-09-09変更：作成初期色は色1＝黒（線・枠・文字）、色2＝白（塗り）のベタ2色へ統一。ツール下から変更可能、起動時にリセットし永続化しない。オブジェクトの設定変更から作成初期色への引継ぎは廃止。新規図形はベタ、グラデーション・テクスチャは作成後に設定する。以前の作成色引継ぎ方針より本項を優先する。

- 2026-09-09：新規Rectangle／Line／Pathのrecord全体を初期化し、未初期化GroupIdによる意図しないグループ化を修正。作成色はラベルなしの丸2つ＋交換矢印へ変更。左上＝線・枠・文字、右下＝塗り。108回の作成とUndo／Redo、設定UI、グループ、本体ライフサイクルを検証。通常EXE更新済み。

- 2026-09-09：四角Layerの既定線幅とEditorState未接続の追加時線幅を1へ統一し、既定で枠＋塗りとする。明示した塗りのみモードは維持。作成経路・描画・保存のテスト7件通過、通常EXE更新済み。

- 2026-09-09：四角サムネイルのFrameRectが線色ではなく塗り色を使っていた不具合を修正。GDI／Direct2DともPenで輪郭を描画し、白塗り＋黒枠を画素検証。通常EXE更新済み。

- 2026-09-09：同分類（線／図形／文字）の単一選択から新規作成へ属性を引継ぐ。異分類・未選択・複数選択は初期値。Core/Appearance/ObjectAttributesへ汎用の取得／適用を分離し、初期色自体は変更しない。以前の「新規は常にベタ」より本項を優先。詳細は実装ノート末尾。

- 2026-09-09：図形の影をモデル・描画・影タブ・Undo・MIF／SVG／JSON・作成属性引継ぎ・サムネイルへ接続。提供赤影の属性と本体寸法を検証。Skiaによるぼかし境界は元アプリと完全一致ではない。詳細は実装ノート末尾。

- 2026-09-09：影ページにも既存ページの表示欠け対策（二重バッファ継承停止、TStaticText、背景・フォント明示）を適用。ダークテーマでタブ巡回・リサイズ・再生成後の実表示を確認。通常EXEは起動中で上書き不可。修正版はTestOutput/AppDebugへ出力済み。

- 2026-09-09：設定欄のPageControl／TabSheetを廃止し、カテゴリのコード描画アイコン＋選択パネル1枚のみ表示へ変更。選択強調・ツールチップ・狭幅折返し・矢印キー対応。実クリックと従来設定テスト通過、通常EXE更新済み。

## 設定UIの共通規則（2026-09-09）

- 設定ページの文字はUI/VectArtDesignerSettingsFontの共通定数を使う。ラベル・入力欄・カテゴリ名・色設定ポップアップをSegoe UI、Height=-13（96 DPI基準）で統一し、ページごとに文字寸法を指定しない。アイコンとして描く記号は図柄として扱う。
- 今後の数値設定UIには、線形グラデーションの「角度」で使用しているスライダー＋数値入力の組合せを積極的に使用する。既存のTVectArtNumericSliderを再利用し、範囲・刻み・小数桁・必要に応じたスライダー範囲を設定する。値同期と変更通知の仕組みも共用する。

- 設定ページは「要素名：UI」の横並びを基本とし、ラベルの右に入力欄・スライダー・ボタン群を置く。縦積みの見出しと入力欄を避け、文字列の複数行入力だけ必要な高さを確保する。

- 情報ページは例外として従来のX／Y・幅／高さの2列配置を維持し、透明度のラベルとスライダーは上下に置く（2026-09-09修正）。

- ビルドごとの一通りの検査は禁止。変更や不具合に必要な検査だけを選んで実施する（ユーザー指示）。

- 2026-09-09：SYNC_ScreenLayoutの暗色縦スクロールバーをレイヤー一覧へ移植。行が表示領域を超えた場合に右端へ表示し、ホイール・つまみ・ページクリック・キーで移動できる。下端基準の積層順、グループ展開、スクロール後の行操作へ接続。限定テストと本体検証ビルド通過。

- 2026-09-09：情報ページの「位置・サイズ (px)」見出しを撤去し、座標・寸法を上へ詰めた。透明度は「名称：バー：数値入力」の1行配置へ変更。設定UIテスト、実画面確認、本体検証ビルド通過。

- 2026-09-09：設定カテゴリのアイコン切替を廃止し、既存パネルを単一の縦スクロールページへalTopで積み上げた。利用可能な全カテゴリを同時表示し、カテゴリ見出しと1pxベベルで区切る。選択変更時は全表示パネルを再描画する。レイヤーパネルのドック接続前にスクロールバー更新が走る親未接続エラーも修正。限定UI／ライフサイクル検査通過。

- 2026-09-09：設定欄の縦スクロールバーへホバー中のホイール操作を明示的に処理。本体ビルドのみ確認し、追加テストは実施していない。

- 2026-09-09：左ドックへ独立したテンプレ図形枠を追加し、従来のテンプレ選択ウィンドウを埋め込みUIへ変更。テンプレ専用の2色設定を廃止し、一覧と配置はツール下部の共通Color1／Color2を使用する。本体ビルドのみ確認し通常EXE更新済み。

- 2026-09-09：テンプレ図形枠のFrame用DFMが未追加で発生していた実行時の「リソースが存在しない」エラーを修正。最小DFMを追加し、本体ビルドのみ確認して通常EXEを更新した。

- 2026-09-09：TemplatePicker生成中に親枠のウィンドウ確立前からComboBoxを初期化していたため「ウィンドウを持っていない」例外が発生。子UI生成をCreateWndまで遅延し、生成前のRefreshも安全化。本体ビルド成功。通常EXEは起動中で上書き不可、修正版はTestOutput/AppDebugへ出力済み。

- 2026-09-09：テンプレ図形フレームの既定横幅を290pxから半分の145pxへ変更。

- 2026-09-09：キャンバス／レイヤー一覧共通の右クリックメニューへ「重なり」を追加。最前面、最背面、1段前面、1段背面への移動を実装し、複数選択とUndoを既存レイヤー操作で維持。本体ビルドのみ確認し通常EXE更新済み。

- 2026-09-09：共通の右クリックメニューへ「整列」を追加。複数選択の外接範囲を基準に左／左右中央／右／上／上下中央／下／中央へ整列する。回転済み図形を含む全対応レイヤー種別とUndoに対応。本体ビルドのみ確認し通常EXE更新済み。

- 2026-09-09：共通の右クリックメニューへ切り取り／コピー／貼り付け／削除／複製を追加。コピーは選択オブジェクトの独自JSONと背景透明の選択範囲PNGを同時格納。貼り付けは独自形式を編集可能レイヤーとして優先し、外部PNG／Bitmap／DIBは中央配置のImageへ変換。Ctrl+X/C/V/DとDelete、Undoに対応。本体ビルドのみ確認。

- 2026-09-09：クリップボード操作追加時にDeleteキーを既存登録と重複して追加し、起動時に「Shortcut is already registered」が発生していた。新規の重複登録を除去し、従来のDelete処理を継続使用。

- 2026-09-09：ImageをDeleteした直後、レイヤー一覧のサムネイルキャッシュ同期が解放済みImageLayerを辞書キーとして参照していた実行時エラーを修正。キャッシュキーを安定したLayerIdへ変更。

- 2026-09-09：文字列選択時の設定積み上げ順を変更し、共通の「情報」を文字色／文字／縁取り／効果の後、一番下へ配置。図形選択時の順序は維持。

- 2026-09-09：右クリックメニューの通常貼り付けと削除の間へ「オブジェクト属性の貼り付け」を追加。色／サイズ／文字／フォント名／縁取り／効果の入口を再現。選択的な属性適用は未実装のため各項目は無効表示。

- 2026-09-09：文字設定の「情報」が上へ戻る問題を修正。alTopホストを一旦alNoneにしてTopを文字色／文字／縁取り／効果／情報の順に設定してからalTopへ戻す。文字入力中は表示構成・高さ・見出しが不変なら再整列と全体Invalidateを省略し、設定欄のちらつきを抑制。

- 2026-09-09：キャンバス上の文字入力による文書変更時は、レイヤー一覧・情報・ツールバー等の更新を入力停止200ms後へ遅延・集約。複製はRectangle／Line／Path（開閉・Bezierを含む）／Image／Textと混在選択へ対応し、属性・重なり順・グループを維持して24pxずらす。1回のUndo／Redo対象。

- 2026-09-09：`SYNC_ScreenLayout/ScreenDesignMaker.dpr` のレイヤーD&Dを参考に、レイヤー一覧での並べ替えへ対応。5px移動で開始し、行の上下半分による挿入位置、挿入線、上下端の自動スクロール、複数選択・フラットグループの一体移動、Undo／Redoを実装。本体ビルドのみ確認。

- 2026-09-09完成整理：Clipboard／複製で重複していたレイヤーデータ取得をCore/Transferへ集約し、LayerListのD&D順序計算・履歴をLayers/Interactionへ分離。DPR／DPROJ／Source READMEを更新。コメントはユニットの目的・担当範囲と、画像所有権・グループ境界・LayerId・文字更新遅延の意図と制約に限定した。

- 2026-09-09：キャンバスの切り取り領域ツールを追加。四角／丸／鋭角の閉じた図形／閉じた自由曲線をボタン再クリックまたはKで循環する。確定後は一時選択だけを保持し、Ctrl+Cでキャンバス内の交差範囲をPNG／Bitmapへコピーする。非四角形の外側は透明。Document・Undo・独自オブジェクト形式には追加しない。限定3テストとDebug本体ビルド通過。

- 2026-09-10：16384pxを超える大きな切り取り領域をレンダラー上限内へ縮小し、形状マスクも出力倍率へ追従するよう修正。関連3テスト通過。ネイティブ画面の自動操作は実行環境の操作口が無効なため未確認。

- 2026-09-10：右クリックの「オブジェクト属性の貼り付け」へ色／サイズ／文字／フォント名を実装。独自形式でコピーした単一オブジェクトを元に、互換対象への複数選択適用と1回のUndo／Redoへ対応。色は線幅や塗り有効状態、文字とフォント名は別属性、サイズは各対象の中心を維持する。縁取り／効果はモデル未実装のため無効のまま。属性貼り付けテスト、既存FillIntegrationTests、Debug本体ビルド通過。

- 2026-09-10：四角切り抜きはコピー機能ではなく、ドラッグ中／確定後の点線枠だけが見えない問題だった。FrameRectがPenではなく透明Brushを使っていたため、四角の画面輪郭も閉じたPolylineとして楕円以外と同じPen描画へ統一。先行調査で加えたモード初期化・削除処理変更は取り消した。

- 2026-09-10最終調整：独立したテンプレ図形枠と重複する☆アイコンをツールパレットから削除。レイヤー一覧は名称・サイズ・透明度・グループ件数の文字表示を止め、可視／ロックとサムネイルだけにした。左ドック総幅を変えずLayersを224→170px、テンプレ図形を145→199pxへ再配分。関連2テストとDebug本体ビルド通過。

- 2026-09-10：キャンバス設定ダイアログを日本語化。幅・高さの数値入力欄と名称付きプリセット一覧を併設し、プリセット選択を入力欄へ反映する。手入力にも対応し、正の整数でない間はOKを無効化。リストのダブルクリックだけでは確定せず、OK時だけDocumentへ適用する。Debug Win64本体ビルド通過。

- 2026-09-10：線の太さ、透明度、影などの共通スライダー操作中は、キャンバスだけを変更通知ごとに即時再描画し、レイヤー一覧・設定欄・ツールバーの同期再描画を入力停止200ms後へ集約。スライダーのフォーカスから連続入力を判定するため、マウスドラッグ、ホイール、キー操作に共通対応。SettingsUiTests、MainFormLifecycleTests、Debug Win64本体ビルド通過。

- 2026-09-10：オブジェクト設定の線幅スライダーと数値入力の最小値を1pxへ変更し、0へ設定できないよう修正。「塗りのみ」は外観モード側で線幅0を使う既存仕様を維持。SettingsUiTestsへ下限検証を追加し、Debug Win64本体ビルド通過。

- 2026-09-10：ファイルメニューを拡張。「新規キャンバス」（Ctrl+N）は1920x1080、「ウィザードで新規作成」（Ctrl+Shift+N）はキャンバス設定で確定したサイズの空Documentへ初期化する。開く／保存に成功した最大10件を［ファイル］→［履歴］へ新しい順で表示し、設定ファイルへ永続化。上書き保存、名前を付けて保存、履歴消去、上部Newボタンも接続。FileActionsUiTests、MainFormLifecycleTests、Debug Win64本体ビルド通過。

- 2026-09-10：Document内容のRevisionが変化するとフォームタイトル末尾へ`*`を表示し、保存・読込・新規作成後に解除する。選択変更だけでは表示しない。終了、新規キャンバス、ウィザード新規作成、ファイル読込の前に「はい／いいえ／キャンセル」で保存確認し、はいの保存失敗・保存ダイアログ中止時は後続操作も中止する。指示によりライフサイクルテストは行わず、Debug Win64本体ビルドのみ実施して警告・エラー0。

- 2026-09-10完成整理：MainFormから未保存状態／タイトル／保存確認をShell/Document/DocumentSessionへ、SVG／MIF入出力とMIFコンテナー所有権をShell/File/DocumentFileControllerへ分離。FileActionsUI内の履歴順序・上限・INI往復はShell/File/RecentFilesへ分離し、MainFormを1321行から1129行へ縮小した。新設ユニット先頭には目的と担当範囲、処理内には非対話時に確認を暗黙承認しない制約だけを記載。DocumentFileControllerTests、DocumentSessionTests、FileActionsUiTestsとDebug Win64本体ビルドが通過。指示に従いライフサイクルテストは実施していない。

- 2026-09-10：キャンバス設定へ白／黒／透明／任意色と埋め込みHSVピッカーを追加。透明時はピッカーを表示したまま無効化し、編集面は既存の市松模様を使用する。MIF背景textureの`texture color1`と`image alpha`で背景色・透明を往復し、独自キーは追加しない。透明MIFの元アプリ表示は未確認。

- 2026-09-10：キャンバス設定の色見本がダークStyleで黒く固定表示される問題を独自描画Swatchで修正。編集キャンバス外側を中間灰色`#808080`へ変更し、白／黒のどちらでもキャンバス境界を識別できるようにした。

- 2026-09-10：縦長になったキャンバス設定を圧縮するため、サイズプリセット一覧をコンボボックスへ変更。手入力とのプリセット同期は維持し、ダイアログ高を574pxから440pxへ縮小した。

- 2026-09-10：上部ショートカットバーを42pxから36pxへ縮め、アイコンはバー高に応じて垂直中央へ配置するよう変更。レイヤーサムネイル背景は市松模様を既定とし、既存の`ThumbnailBackground`指定で白へ戻せる切替口は維持した。LayerRendererCanvasHiddenTestsとDebug Win64本体ビルドが通過。

- 2026-09-10：画像出力を実装。［ファイル］→［出力］は全オブジェクト、オブジェクトの右クリック→［出力］は選択中だけを対象とし、双方に［ファイル...］と［クリップボード］を設けた。出力寸法と切抜き範囲は常にキャンバス実寸。ファイルはPNGを既定としてGIF／JPEGも選択でき、PNGはCanvasの透明を維持、GIF／JPEGは透明部分を白へ合成する。クリップボードはPNG。既存のコピーはキャンバス外を含む選択範囲PNGと編集用JSONの経路として維持。PngOutputTests、FileActionsUiTestsとDebug Win64本体ビルドが通過。

- 2026-09-10：［出力］サブメニューをファイルメニューと右クリックメニューの最下段へ移動し、直前に区切り線を配置した。

- 2026-09-10完成整理：Canvasに同居していた作図中の線種・cap・端点マーカーのGDI／Direct2Dプレビュー263行を`Editor/Rendering/LinePreview`へ分離し、Canvasを1998行から1766行へ縮小。画像出力はキャンバス描画・背景合成・クリップボードを`Shell/File/PngOutput`、PNG／GIF／JPEG変換とディスク書込みを`Shell/File/ImageFileEncoder`へ分離した。Sourceのフォルダ別ユニット数は最大6で、大量同居するフォルダはないため追加の移動は行わない。全Sourceユニットの先頭に目的・担当範囲コメントがあることを確認し、AttributePasteOperationsの不足を補完。関連3テストとDebug／Release Win64本体ビルドが通過。

- 2026-09-10：色履歴をドキュメント単位の最大16色としてCore/Appearanceへ分離。新規作成時は空、MIF／SVG読込時は透明背景と画像／テクスチャ画素を除く背景・塗り・線・文字・影・グラデーション色を重複なしで収集する。ピッカー操作中は履歴を更新せず、非アクティブ化、終了、対象／色スロット切替時に最終色だけを先頭へ確定。基本16色は常設。ColorHistoryTests、SettingsUiTests、PaintPopupLifetimeTests、MainFormLifecycleTestsとDebug／Release Win64本体ビルドが通過。

- 2026-09-10：キャンバス編集を伴うデバッグで画面確認に阻まれないよう、Debugビルドは終了・新規・読込前の未保存確認を表示しない。Releaseビルドでは通常の保存確認を維持する。

- 2026-09-10パターン準備：`D:\DelphiProg\test\SYNC_ScreenLayout\ScreenDesignMaker.dpr`の完成済みパターン実装を確認し、斜線、ドット、格子、市松、波線、ハニカムの形状生成を`Rendering/Paint/Pattern/VectArtDesignerPatternTiles`へ移した。VectArtDesignerでは選択時に固定寸法のPNGへ確定して既存の画像テクスチャとして扱う方針のため、参照側の設定保持モデル、JSON、スライダーUI、Undoジェスチャー、倍率別キャッシュはコピーしていない。現時点ではUIとDocumentへ未接続の準備ユニットとし、次回は生成PNGを`vfkTexture`へ渡すだけでMIFの埋込画像経路を利用できる。PatternTileTestsによる6種類のPNG生成・再復号と、Debug／Release Win64本体ビルドが通過。

- 2026-09-11パターン設定：`SYNC_ScreenLayout`のパターン設定を共通ペイントポップアップへ適合させた。テクスチャ欄で画像または斜線、ドット、格子、市松、波線、ハニカムを選択し、前景／背景色と種類別の線幅、間隔、サイズ、振幅、周期、および角度、横／縦移動を編集できる。操作中は512px角のPNGを再生成して即時適用し、確定後は従来の`vfkTexture`として描画、Undo、MIF／SVG保存経路を共有する。MIFへアプリ固有設定を追加しないため、保存・再読込後は通常の画像テクスチャとなり数値設定は復元しない。PatternTileTests、SettingsUiTests、TexturePaintTestsとDebug／Release Win64本体ビルドが通過。

- 2026-09-11パターンUI調整：画像選択ボタンと色欄の重なりを解消し、パターン時は色履歴を省いたコンパクトな設定とHSVピッカーだけを表示する。動的ラベル、種類コンボ、色スロットへダーク配色を明示し、保存用512px画像とは別の表示寸法でプレビューを生成して縮小による模様潰れを防いだ。SettingsUiTestsの単体キャプチャで全項目の文字、斜線プレビュー、画面内への収まりを確認した。

- 2026-09-11パターン初期色：内蔵パターンを選択した時点で、選択オブジェクトの色ではなくツールパレットのEditorState.Color1／Color2を前景色／背景色へ採用する。選択後の2色は従来どおりポップアップ内で個別編集できる。

- 2026-09-11パターン表示調整：パターン種類コンボの文字を2px大きくし、コントロール高を4px増やして文字切れを防いだ。数値スライダー操作中は全UIのSyncを行わず、操作対象の値、パターン画像、上部プレビュー、Documentだけを更新する。
