# VectArtDesigner 作業ノート

作業再開時に最初に確認する現在方針を扱う。実装状況と次の作業は
[note_VectArtDesigner.md](note_VectArtDesigner.md)、完了済み作業は[HISTORY.md](HISTORY.md)へ記録する。

## 製品方針

- `VectArtDesigner`は単体で動作するIBM WebArt Designer互換アプリとする。
- 互換性の基準はMIFファイルを読み書きできることだけでなく、WebArt Designerのオブジェクト構造、
  編集操作、プロパティ設定と保存結果の対応を再現できることとする。
- 当面は作図・編集ツールとしての機能を優先して充実させる。各ツールの実装時にWebArt Designerの
  実際の操作と、操作前後に生成されるMIFの差分を調査する。
- WebArt Designerの全機能を複製するのではなく、調査結果から本アプリに不要な機能を明示して対象外とする。
- AviUtl2プラグイン、画面レイアウトプラグイン、グラフプラグインとの共有を前提にしない。
- MIFを主保存形式とし、名前を付けて保存する場合もMIFを既定にする。
- SVGの読込と保存は維持するが、Documentと編集UIが扱う表現はMIFへ変換できる範囲に限定する。
- SVG固有表現を内部モデルへ増やさず、MIFとSVGの往復で共通して保持できる値を正本とする。
- 標準モードとMIF互換モードの区別は設けない。編集体系は常にMIF仕様とする。
- 線種、品質、始点／終点マーカーなどは通常のDocumentデータであり、型・プロパティ・UI・Undo名へ
  `Mif`接頭辞を付けない。MIFコンテナーやMIF変換処理など、ファイル形式固有の名前には`Mif`を残す。

## モデルとUI

- 中央編集、レイヤー、編集ツール、オブジェクト設定を独立した`TFrame`として構成する。
- 各Frameは`IVectArtDesignerContext`を通じてDocument、編集履歴、EditorStateへ接続する。
- Canvasはレイヤー一覧へ表示せず、`編集`メニューの`キャンバスの設定`から変更する。
- ダークモードを基本とし、アイコンはコード描画する。
- レイヤー名を内部識別へ使用しない。変更されない識別情報が必要になった場合はモデルへ明示的に追加する。
- Lineと開いたPathのマーカーはレイヤーサムネイルへ描画し、キャンバス上でもクリック選択の対象にする。
  Pathの選択枠は頂点編集とリサイズの基準なので、装飾マーカーの外形では拡張しない。
- WebArt Designerの2つの手書きツールは、どちらもドラッグ軌跡をペンを離した時点でPathへ変換する。
  一方は軌跡を鋭角頂点の連続直線へ、もう一方は連続ベジェ曲線へ変換するため、両方を実装対象とする。
- ツールパレットでは連続直線／連続ベジェを1ボタン、手書き直線／手書きベジェを1ボタンにまとめる。
  選択中のボタンまたは対応キーを再入力するとペア内を切り替える。キーは選択`S`、直線`L`、
  連続線`P`、手書き`B`を基本とし、`B`が将来の機能と競合した場合だけ割当を見直す。
- 派生図形は専用のDocument型を増やさず、閉じたPathの頂点データをプリセットとして生成する。
  優先度は低く、基本作図ツールの後に扱う。

## 描画

- Documentの描画は`Source/Rendering/VectArtDesignerRenderer.pas`へ集約する。
- 編集背景、選択枠、操作ハンドルなどの補助表示にはDirect2Dを使用し、利用できない場合はGDIへ
  フォールバックする。
- 大きな文字と装飾文字には`Lib/TextRenderer`のGoogle Skiaベース描画を使用する。
- Win64ランタイムは`Lib/Skia/Win64/sk4d.dll`へ配置する。

## 永続化

- MIFの確認済み構造は[MIF解析メモ](mif_analysis.md)を正として参照する。
- Reader／Writer往復、バイト差分、構造表示には[MIF診断ツール](Tools/MifDiagnostics/README.md)を使用する。
- MIFは対応内容だけをDocumentへ取り込み、未知情報が保存時に失われる場合は書出しレポートで通知する。
- MIF書出しではアプリ固有情報を新規追加しない。既存ベクターペイロードなどは可能な範囲で再利用する。
- 互換性判定とコンテナー生成は同じ処理で行い、完全対応、変換あり、非対応を生成結果と同時に返す。
- `TryAnalyzeVectArtMifExport`でMIFを生成せず同じ互換性レポートを取得できるようにし、編集中の
  ステータス表示と保存確認で判定内容を共有する。
- SVGはMIF編集モデルの交換形式とし、Rectangle、Line、Path、Imageと対応する装飾だけを扱う。
- SVGの対応表は[SVG入出力のMIF編集互換範囲](svg_compatibility.md)を正とし、変換・無視した
  描画要素は`TSvgImportReport`で読込後に通知する。
- MIFコンテナー内の既存Document JSONキーは、過去データを読めるよう読み書き互換性を維持する。

## コードとビルド

- Delphi 37.0、Win64を使用し、DebugとReleaseを検証する。
- Windows専用VCLアプリとして`Windows Modern Dark` VCL Styleを基本にする。標準VCLで正しく
  ダーク化できないコントロールだけ独自描画またはStyle Hookで補う。
- メニュー、ダイアログ、一般的なボタンと入力部品は標準VCLを優先する。スクロールバーと
  トラックスライダーは操作・描画仕様を維持するため独自UIとする。
- コンパイル警告0、エラー0を原則とする。
- ビルド成果物はGitへ追加しない。
- 日本語文字列リテラルを持つ`.pas`と`.dpr`はUTF-8 BOM付きで保存する。
- ファイル編集後は`git diff --check`を実行する。
- ユニット先頭には目的と担当範囲を記載し、コメントは処理の言い換えではなく意図や制約を書く。

## 実装時の参照先

- `D:\DelphiProg\test\SYNC_ScreenLayout`では類似するベクター編集機能を実装しているため、
  ツール、モデル、作成操作、キャンバス操作、描画、プロパティUI、Undo／Redo、テストを都度参照する。
- 特に楕円、角丸四角形、円弧、自由線、ベジェPath、文字、変形処理は同プロジェクトの実装を確認する。
- 参照先のAviUtl2連携、プラグイン用公開口、独自JSON仕様は持ち込まず、VectArtDesignerの
  単体アプリ方針とWebArt Designer互換MIFモデルへ合わせて実装する。

## 現在の次作業

1. 次は既存の`丸.mif`で構造を確認できる楕円ツールを実装候補とする。
2. その後は曲線系、文字、塗り系をWebArt Designerの操作とMIF構造を確認しながら順に調査・追加する。
3. 各ツールについてUI設定、MIFメタデータ、再読込結果の対応表を作る。
4. 調査した機能を互換対象、変換対象、不要機能に分類する。
5. 派生図形を閉じたPathの低優先度プリセットとして追加する。
6. MIF互換性の詳細をステータスヒント以外から常時開けるUIが必要か確認する。
7. MIF特化後も残っている不要な外部ホスト向け公開口とコメントを整理する。
