# VectArtDesigner 制作履歴

## 2026-09-08 設定UIの初期実装

ユーザー指定の「UIを先に揃え、対応済み項目から動かす」に従った第一段階。
下記の仕様本文は最終目標であり、この節を現在の実装状況の正とする。

- オブジェクト設定を情報・文字・線・塗り色／文字色・枠色・影・縁取り・効果のタブへ整理。
  選択種類に応じて表示し、未対応の文字自動サイズ・行揃え・配置等は準備中として無効表示する。
- 既存の位置・サイズ・不透明度・線設定に加え、文字列・フォント・サイズ・文字スタイルを
  既存DocumentとUndoへ接続。Lineの端／結合・品質・マーカー設定も接続した。
  縦横比固定はRectangle／Textに対応。枠のみ・塗りのみ・枠＋塗りはRectangle／閉じたPathに対応。
- 色見本から共通の非モーダルポップアップを開く。ベタ色はHEX／RGB、使用色、最近の色、
  プリセットから即時反映しUndo可能。グラデーション方向・2色と画像テクスチャはプレビューのみ。
- 影・縁取り・文字効果は種類別の設定欄を用意。これらの欄はローカルな試入力で、
  オブジェクトへの適用・保存はしないことを画面に明示する。
- ツールパレットへテンプレ図形ボタンを追加（合計11）。9カテゴリ30種を現在の枠・塗りで
  プレビューし、ドラッグで通常の閉じたPathとして配置。Shiftで1:1、Altで中心基準。
  初期図形は多角形／曲線の点列近似で、配置後は通常Pathとして編集・Undo／Redoできる。
- 次段階：グラデーション／テクスチャ／影／文字装飾の描画とMIF往復、塗り設定全体の履歴再利用、
  文字自動サイズ・配置、定型図形の全候補・ベジェ制御点・検索・お気に入り等。
- 実装入口：ObjectProperties/VectArtDesignerObjectSettingsUI.inc、VectArtDesignerPaintPopup.pas、
  VectArtDesignerSettingsDrafts.pas、ToolPalette/VectArtDesignerTemplatePicker.pas、
  Editor/Geometry/VectArtDesignerTemplateGeometry.pas（すべてSource配下）。
- 検証：Win64 Debug／Releaseビルドは警告・エラーなし。SettingsUiTests、PathStylePropertiesTests、
  TextEditingTests、RoundedRectangleCreationTests、SnapInteractionTestsがPASS。
  SettingsUiTestsは実Frame生成、色／文字／Line変更、ロック、Undo／Redo、30種の図形範囲を確認し、
  Win64/UiTestsへダークスタイルの画面PNGを出力する。


完了済みの作業と、日付付きの重要な検証結果を記録する。
共通基盤の状況は [note.md](note.md)、単独編集アプリ固有の状況は
[note_VectArtDesigner.md](note_VectArtDesigner.md) を参照する。

## 2026-09-08 スナップ操作

- SYNC_ScreenLayoutの候補探索を左上原点・フラットグループへ適合させ、移動、通常リサイズ、
  Line端点、Path頂点、基本図形作成へ接続した。辺・中心・同一Path内の別頂点をグリッドより優先する。
- Alt一時解除、作成時Shift水平・垂直拘束、回転の30度系・45度系近接吸着とShift 15度刻みを追加した。
- Direct2D／GDIの点線ガイドと対象枠、回転吸着時のハンドル色を追加した。
- SnapInteractionTestsで候補優先、ズーム許容距離、左上原点、グループ相対配置、頂点編集、
  リサイズ、Alt解除、作成、Undo／Redoを検証した。既存7本の関連テストも通過した。
  GroupMixedInteractionTestsは変形精度の検証としてAlt解除を明示し、吸着は専用テストへ分けた。
- Win64 Debug／Releaseは警告0・エラー0。回転図形・画像単体のリサイズ吸着は後続対応とする。

## 2026-09-08

- グループ行の開閉表示を常時見える四角い`＋／－`ボタンへ変更した。ボタンはシングルクリック、
  行全体は従来どおりダブルクリックで開閉し、折り畳み中・展開中のどちらでもグループ行と状態を判別できる。
- レイヤー一覧のグループ展開方向を見出しの下側へ変更した。展開中はグループ見出しから最下部の
  メンバー行までをアクセント色の外枠で囲み、グループ範囲を明示する。表示行順と範囲矩形を
  `LayerGroupDisplayTests`で確認した。
- 右クリックのグループ操作を`グループ`サブメニューへまとめ、子項目を`グループ化`と
  `グループ解除`の2件にした。解除を戻す専用の再グループ化は追加せずUndoへ統一し、同一グループを
  再度グループ化して不要な履歴を作る操作も無効にした。
- 通常レイヤーとグループ合成サムネイルの背景を白へ変更した。`ThumbnailBackground`設定には
  市松模様を残し、背景変更時は合成サムネイルキャッシュを再生成する。描画テストで白の既定値と
  市松模様への復帰を確認した。
- レイヤー一覧でフラットグループを1個のグループ行へ集約し、ダブルクリックによるメンバー行の
  展開・折り畳みを追加した。参照先の表示行キャッシュ方式をフラットモデル向けに適用し、グループ行の
  サムネイルは全メンバーを実配置と積層順のまま合成する。表示・ロックは全メンバーへ一括適用して
  Undo／Redoできる。`LayerGroupDisplayTests`で行数、展開状態、一括表示切替と履歴を、
  `GroupThumbnailRendererTests`で複数メンバーの合成を確認した。
- フラットな`GroupId`へ選択レイヤーをまとめるグループ化と解除を追加し、`Ctrl+G`、
  `Ctrl+Shift+G`、共通右クリックメニューへ接続した。メンバー1件の選択はグループ全体へ展開し、
  既存グループとの再グループ化は入れ子を作らず新しい1グループへ統合する。グループ化・解除、
  削除復元、同種グループの複製は所属を維持してUndo／Redoできる。既存の複数選択移動・リサイズを
  LineとPathへ拡張し、Rectangle、Text、Imageとの混在グループも一体で変形できるようにした。
  `LayerGroupOperationsTests`と`GroupMixedInteractionTests`で選択、再グループ化、ロック、移動、履歴を確認した。
- 階層化しないグループ化の第1段階として、各レイヤーへ実行時に不変の`LayerId`、フラットな
  `GroupId`、描画内容単位の`Revision`を追加した。DocumentはIDから現在のレイヤー位置、GroupIdから
  積層順の所属レイヤー列を取得する内部キャッシュを遅延再構築する。通常の1レイヤー編集では対象の
  Revisionだけを更新し、追加・削除・並べ替えでは関係キャッシュだけを失効させる基盤とした。
  `LayerRelationCacheTests`でID、所属変更、並べ替え後の再構築、非対象Revisionの維持を確認した。

## 2026-09-07

- 非表示オブジェクトをレイヤー一覧から選択した場合もキャンバスへ選択枠と操作ハンドルを表示するようにした。
  オブジェクト右クリックメニューを共通部品化し、レイヤー一覧の右クリックからも同じ非表示、反転、回転を
  実行できるようにした。選択済みの行では複数選択を維持し、未選択の行ではその行を対象に切り替える。
- 選択オブジェクトの右クリックメニューへチェック付きの「非表示」を追加した。レイヤー一覧と同じ
  `Visible`状態を切り替え、複数選択は一括操作を1件のUndo／Redoとして扱う。
- 作図直後の右クリックを選択モードへの切替とオブジェクトメニュー表示へ接続した。カーソル直下に
  オブジェクトがなくても現在選択を対象にし、Path／Bezierは右クリック確定後の同じ操作でメニューを開く。
- オブジェクトの右クリックメニューへ「回転 > 左へ90度／右へ90度／180度」を追加した。単一選択は
  自身の中心、複数選択は選択全体の中心を基準に全対応オブジェクトを回転し、1件のUndo／Redoで扱う。
  任意の角度指定は設定状態表示側で操作する方針とし、右クリックメニューには追加していない。
- 選択オブジェクトの右クリックメニューへ「反転 > 上下反転／左右反転」を追加した。単一選択は自身、
  複数選択は選択全体の外接範囲中心を共通軸にし、Rectangle、Ellipse、Line、Path、Image、Textを
  1件のUndo／Redoで反転する。文字の字形反転はDocument、描画、入力キャレット、JSON、SVG、MIFへ
  接続した。右ドラッグのキャンバス移動は維持し、見本にある他の右クリック候補も実装ノートへ記録した。
- Textへ後から切替可能な縦書きフラグを追加した。現在のフォントサイズ、字間率、行間率を使って
  右から左の列へ再レイアウトし、縦書き用`@`フォントが利用可能なら描画時に自動選択する。
  Object Properties、クリックキャレット、Undo／Redo、JSON、SVG `writing-mode`、
  MIF `logo writing mode`と表示PNGへ接続した。
- エクスプローラーから編集キャンバスへ画像ファイルをドロップし、Imageレイヤーとして配置できるようにした。
  WICで入力画像をPNGへ変換してDocument内へ複製し、取込元のフルパスは参照情報として別途保持する。
  元ファイル削除後も埋め込み画像で表示・編集でき、複数ファイルの一括配置とUndo／Redoにも対応した。
- Textのリサイズを、最長行の文字幅と字間率、行数と行間率から求めた基準レイアウトに対する
  横・縦それぞれの変形へ変更した。Object Propertiesに字間(%)・行間(%)を追加し、編集、Undo／Redo、
  JSON／SVG保存、MIF表示PNG、クリック位置とキャレット描画へ同じ計算を適用した。
- TextレイヤーとTextツールを追加した。キャンバスクリックで入力を開始し、既存文字のクリック位置へ
  キャレットを移動できる。Enter改行、Escapeキャンセル、外側クリック確定、Windows IME未確定表示、
  Backspace／Delete、サロゲートとCRLFを分割しない左右移動を実装した。
- 複数行TextをSkia共通レンダラー、選択・移動・リサイズ、レイヤー一覧、複製・削除、Undo／Redo、
  Object Propertiesの位置・寸法・文字色・不透明度へ接続した。JSONとSVGの`text`／`tspan`往復も追加した。
- `文字.mif`の`object type=logo`を解析し、UTF-16LE文字列、Shift-JIS補助文字列、フォント属性、色、
  透明度、表示、回転をTextとして読み書きするようにした。元サンプル取込と新規TextのMIF往復を検証した。
- 角丸四角の選択時に28個のPath頂点ハンドルを表示せず、四角と同じ外接枠ハンドルで移動・変形する
  編集属性を追加した。角丸半径の個別操作は提供せず、作成時の輪郭全体を拡大縮小する。
- 図形ツール群の完成整理として、角丸四角の輪郭点列と固定1:8:1半径計算を
  `VectArtDesignerRoundedRectangleGeometry`へ分離した。作成制御は入力状態とDocument確定に絞り、
  追加仕様の意図と互換制約をユニット先頭および判断箇所のコメントへ記録した。
- `Source`配下は既存の責務別フォルダごとに最大9ユニットで、参照パス変更に見合う過密状態ではないため
  フォルダ再編は行わなかった。全31テストを再コンパイルし、診断用2本を除く29本がPASSした。
  Win64 Debug／Releaseも警告0、エラー0でビルドした。
- Rounded Rectangleツールを独立アイコンで追加した。枠のみ、塗りのみ、枠＋塗りの3モードを
  再クリックと仮キー`U`で循環する。作成時は左右の丸み:中央直線:丸みを1:8:1とし、高さの半分を
  半径上限にする。各角を6分割した閉じたPathへ確定するため、その後の変形では比率を再計算しない。
- Rounded Rectangleの比率、3モード、Undo／Redo、ショートカットのテストを追加した。
- 鋭角のClosed Pathとは別にClosed Bezierツールを追加した。アンカー指定中から滑らかな閉曲線を
  プレビューし、始点クリック、ダブルクリック、右クリックで`Bezier=True / Closed=True`のPathを
  直接作成する。枠のみ、塗りのみ、枠＋塗りの3モードを再クリックと仮キー`G`で循環する。
- Closed Bezierの作成、3モード、Undo／Redo、ショートカットと、MIFで閉じた連続直線へ変換した後も
  閉鎖・塗りを維持する回帰テストを追加した。全30本を再コンパイルし、診断用2本を除く28本がPASSした。
- WebArt Designer 21のローカルヘルプと付属6,603 MIFを調査した。専用ツールはないが
  `編集 > 曲線を閉じる`で始終点を直線接続でき、type 9/11の3次ベジェ命令を持つ
  `vector closed=1`実例も確認した。閉じた曲線はネイティブ曲線読書きの後に対応可能と判断した。
- 角丸四角はヘルプにも丸み量の調整がなく、付属MIF群にも角半径キーや専用element typeを
  特定できなかった。輪郭のMIF化は可能と見込むが固定半径と変形規則は不明なため、実装保留とした。
- 鋭角のClosed Pathツールを追加した。頂点を順にクリックし、始点クリック、ダブルクリック、右クリックで
  必ず閉じた直線Pathとして確定する。枠のみ、塗りのみ、枠＋塗りを選択中の再クリックまたは`C`で循環し、
  アイコンと作成結果へ反映する。Document、Undo／Redo、JSON、SVG、MIFは既存Path表現を共有する。
- 角丸四角は元アプリ側に不明な仕様があるため実装順を後ろへ移した。
- Ellipseツールを追加した。枠のみ、塗りのみ、枠＋塗りを選択中の再クリックまたは`E`で循環し、
  ドラッグ中のShiftで幅と高さを同じにして正円を作成する。既存の境界矩形編集を共有しつつ、描画、
  当たり判定、レイヤーサムネイル、Undo／Redo、JSON、SVGの`ellipse`要素へ接続した。
- `丸.mif`を解析してEllipseを`vector element type = 2`として読み書きし、WebArt Designer製の
  106x1 vector IPNGを新規Ellipse用に使用するようにした。元MIFのベクターペイロードも保存時に維持する。
- テスト29本をWin64で再コンパイルし、引数を必要とするMIF診断用2本を除く27本がPASSした。
  単体アプリのDebug／Releaseも警告0、エラー0でビルドした。
- Rectangleツールを枠のみ、塗りのみ、枠＋塗りの3モードへ拡張した。選択中のパレットボタン再クリックと
  `R`の再入力で循環し、アイコンへ現在の枠・塗り状態を表示する。Rectangleの塗り有効状態をDocument、
  共通描画、レイヤーサムネイル、JSON、SVG、MIFの`vector enable fill texture`へ接続した。
- Delphi 37.0標準の`Windows Modern Dark` VCL Styleをプロジェクトへ組み込み、フォーム生成前に
  `TStyleManager.TrySetStyle`で選択する構成へ変更した。
- 独自描画していたファイル、編集、表示メニューを標準`TMainMenu`／`TMenuItem`へ置き換えた。
  ファイル操作、Undo／Redo、キャンバス設定、ツール表示、レイアウト編集の既存処理を接続し、
  ショートカット、有効状態、チェック状態をVCL標準プロパティで表現する。
- Win64 Debug／Releaseを警告0・エラー0でビルドし、起動時にタイトルバーと標準メニューバーへ
  ダークスタイルが適用されることを確認した。ドロップダウンメニューと標準ダイアログは実機で確認する。
- ダーク化のためだけに実装していたキャンバス設定ダイアログの独自ボタンを標準`TButton`へ戻し、
  フォーム、リスト、フローティングツールウィンドウへのDWM／`SetWindowTheme`直接指定を撤去した。
  スクロールバーとトラックスライダーは操作仕様を維持するため独自UIのままとする。
- ツールパレットを選択、直線、連続直線、ベジェ曲線、手書き曲線、Rectangleの順に並べ替え、
  線関係のアイコンをWebArt Designerに近い形へ変更した。連続直線は短い折返し終端、手書き曲線は
  塗りつぶしたペン先を加えて区別した。
- ベジェ曲線ツールを選択可能にし、クリックしたアンカー列を通る滑らかな3次ベジェを自動生成する
  処理を追加した。作成中も曲線でプレビューし、ダブルクリックまたは右クリックで確定して
  既存Pathとして追加する。生成はUndo／Redo対象とし、手書き曲線は引き続き表示のみとする。
- ベジェ生成結果を分割済み点列として保持していた初期実装を修正し、Documentにはユーザーが指定した
  アンカーだけを保持するようにした。キャンバス描画、当たり判定、選択枠、サムネイルではアンカーから
  3次ベジェを再生成し、JSONではベジェ属性とアンカー列を往復する。
- 頂点移動などのドラッグ操作をDocumentの対話更新区間として扱い、マウス移動ごとのキャンバス以外の
  メニュー、レイヤー一覧、オブジェクト設定、ツールバー更新を抑制した。ドラッグ終了時に1回だけ同期する。
- 共通Skia描画バッファと編集用`TBitmap`は寸法が変わらない限り記憶域を再利用するようにした。
  編集キャンバスはDirect2Dで合成・表示するが、図形本体はSkia CPU raster-directで描画している。
- 縮小表示時もDocumentの論理寸法（既定1920×1080）で全体描画していた処理を、実際の表示寸法までに
  制限した。参考プロジェクト`SYNC_ScreenLayout`のCanvas Render Cacheと同じ縮小描画方針を適用した。
- 手書き直線ツールを追加した。ドラッグ中は2画面px以上離れた軌跡点を収集してプレビューし、マウスアップ時に
  Ramer–Douglas–Peucker法で同一直線上の過剰な点を整理して、鋭角頂点を持つ開いたPathとして確定する。
  作成結果は通常PathとしてJSON／SVG／MIFと共通化し、Undo／Redo対象にした。
- 手書きアイコンは従来の太いペン先を維持し、滑らかな曲線部分を鋭角な折れ線へ変更した。
- 手書きベジェツールを手書き直線の隣へ追加した。ドラッグ中は取得軌跡を折れ線で表示し、マウスアップ時に
  同一直線上の過剰な点を整理して、各点をアンカーとする開いた連続ベジェPathへ変換する。
- 2つの手書きアイコンは同じ太いペン先を使い、直線版を鋭角な折れ線、ベジェ版を滑らかな曲線で区別した。
- 選択中のPath頂点などがズーム・パン後も変更前の画面座標へ残る問題を修正した。描画直前にInteractionへ
  最新のキャンバスBoundsとZoomを同期し、図形本体、選択枠、端点・頂点ハンドルを同じ座標へ揃える。
- ツールパレットの連続直線／連続ベジェを1つ、手書き直線／手書きベジェを1つのボタンへまとめた。
  選択中のグループボタンを再クリックすると種類を交互に切り替え、アイコンも現在の種類を表示する。
- `ShortcutAction`へ選択`S`、直線`L`、連続線グループ`P`、手書きグループ`B`を追加した。
  `P`と`B`は同じキーの再入力でグループ内を切り替え、文字入力欄や編集領域外では発動しない。
- 線・ベジェ・手書きツール一式の完成整理として、手書きの点間隔判定とRDP頂点整理を
  `VectArtDesignerFreehandGeometry`へ分離した。ベジェの制御点・表示展開も
  `VectArtDesignerBezierGeometry`に集約し、図形作成ユニットは入力状態と確定処理を担当する構成にした。
- 変更ユニットの先頭コメントを現在の責務と制約に合わせて整理した。`Source`配下は各分類2～8ユニットで
  あるため、保守性より参照パス変更の影響が大きいフォルダ再編は行わなかった。

## 2026-08-31

- 製品方針をMIF特化の単体アプリへ変更し、AviUtl2画面レイアウトプラグインのプロジェクト、専用ユニット、
  共通ABIライブラリ、専用テスト、サンプル、ノートを削除した。
- 標準／MIF互換の編集モード列挙とメニュー切替を撤去し、MIF仕様の編集体系へ一本化した。
- 内部モデル、EditorState、Undo／Redo、UI、描画、JSON／SVG処理の`Mif`付き装飾名を、`StrokeStyle`、
  `AntiAlias`、`StartMarker`、`EndMarker`などの通常名へ変更した。MIF形式固有のコンテナー・変換名は維持した。
- 名前を付けて保存する場合の既定形式と先頭フィルターをMIFへ変更し、SVGはMIF表現範囲の交換形式とした。
- 残るテストプロジェクト25本を再コンパイルし、引数を必要とするMIF診断用2本を除く23本を実行して
  すべてPASSした。単体アプリのDebug／Release Win64ビルドも警告0、エラー0で成功した。
- `編集`メニューへ「モード: MIF互換」「モード: 標準」を追加し、Documentの現在モードだけへチェックを
  表示して相互に切り替えられるようにした。MIF／SVG読込後のモード変更通知もメニュー表示へ反映する。
- キャンバス設定を表示しない外部ホストでもモード2項目を詰めて表示し、メニュー下端から欠けないようにした。
- モード変更通知と同一値の再設定、メニューのチェック更新とクリック切替を自動テストした。単独アプリの
  Debug／Release Win64ビルドは警告0、エラー0で成功した。
- Pathへ線端と接合形式を追加し、作成既定値、単一選択時のアイコン式Object Properties、Undo／Redo、
  共通Skia描画、JSON、SVGの`stroke-linecap`／`stroke-linejoin`、MIFメタデータへ接続した。
- Pathの値往復、外部SVGの継承、WebArt製Pathの読込、描画差、UI操作を自動テストした。単独アプリと
  画面レイアウトプラグインのDebug／Release Win64は警告0、エラー0で成功した。
- PathへMIF互換アンチエイリアスを追加し、作成既定値、Object PropertiesのAAボタン、Undo／Redo、
  塗りと輪郭のSkia描画、JSON、SVGの`shape-rendering`、MIFの`vector quality`へ接続した。
- AA有無の描画差、UI操作、JSON／SVG／MIF往復とWebArt製Path読込を自動テストした。
- 開いたPathへ始点／終点マーカーと各サイズを追加し、作成既定値、Document、Undo／Redo、
  Object Properties、共通Skia描画、JSON、SVG、MIFへ接続した。閉じたPathではマーカーを描画せず、
  Object Propertiesの対応項目を無効化する。
- PathマーカーのUI操作とUndo、開閉による描画有無、JSON／SVG／MIF往復、2頂点PathからLineへの
  MIF変換時の保持を自動テストへ追加した。残るテスト25本を再コンパイルし、引数を必要とする
  MIF診断用2本を除く23本がPASSした。Debug／Release Win64も警告0、エラー0で成功した。
- SVGをMIF編集モデルへ取り込む対応範囲を`svg_compatibility.md`へ明文化した。読込時にせん断Rectangleや
  任意の破線を変換、曲線Path、未対応描画要素、任意マーカー、filter／clip-path／maskを無視として
  `TSvgImportReport`へ記録し、アプリから内容を通知するようにした。テスト25本の再コンパイルと診断用を
  除く23本の実行、Debug／Release Win64ビルドはすべて成功し、警告0、エラー0を確認した。
- MIF生成から互換性判定を`TryAnalyzeVectArtMifExport`として分離し、編集時と保存時が同じ
  `TMifExportReport`を使用するようにした。ステータスバーへ完全互換、変換あり、非対応と件数を表示し、
  詳細な変換・非対応内容をヒントで確認できるようにした。生成前後のレポート一致もテストへ固定し、
  テスト25本のコンパイル、実行対象23本、Debug／Release Win64ビルドがすべて成功した。
- Lineと開いたPathの始点／終点マーカーをレイヤー一覧のGDI／Direct2Dサムネイルへ追加し、閉じたPathでは
  描画しないようにした。キャンバス上のマーカーも対象レイヤーの当たり判定へ含めた。Path選択枠は
  頂点編集・リサイズの基準を変えないため頂点範囲のままとし、サムネイル描画とクリック選択をテストした。
  テスト25本のコンパイル、実行対象23本、Debug／Release Win64ビルドがすべて成功した。
- SVGルートの`viewBox`と`preserveAspectRatio`（`none`、9方向の`meet`／`slice`）をキャンバス座標へ
  適用した。角丸、塗り／線の個別不透明度、fill-rule、線の補助属性、画像の比率指定、図形またはグループの
  filter／clip／mask／合成指定など、MIF編集モデルへ保持できない属性を読込レポートへ追加した。
  テスト25本を再コンパイルし、診断用2本を除く23本がPASSした。Debug／Release Win64ビルドも成功した。

## 2026-08-28

- 縮小表示時だけ線幅を画面上1px以上へ補正する処理を正式採用した。DocumentとMIF／SVG／AviUtl2出力の
  線幅は変更せず、定数`ENABLE_THIN_STROKE_PREVIEW`から補正全体を一括無効化できる。
- 元アプリ製MIFの通常画像とロゴを画像レイヤーとして読み込み、PNG本体、透明度、表示状態、配置4頂点、
  レイヤー順を保持するようにした。4頂点から回転と左右／上下反転を再現してSkiaで描画する。
- `画像.mif`の通常画像3個と`文字.mif`のロゴ1個について、読み込み、描画、MIF再保存後の再読込を
  自動テストした。既存テスト一式と単独アプリ／画面レイアウトプラグインのDebug／Release Win64も
  警告0、エラー0で成功した。
- 画像レイヤーの一覧サムネイルがチェッカー背景のままだったため、保持しているPNG本体を縦横比を保って
  GDI／Direct2Dの両描画経路へ表示した。通常画像MIFを使ったサムネイル描画テストを追加した。
- 画像レイヤーをキャンバス上で選択し、4頂点に追従する選択枠から移動、辺／四隅リサイズ、回転を
  操作できるようにした。回転と反転を保つ4頂点更新をUndo／Redo対象にし、削除と積層順変更にも対応した。
- 画像変形後の4頂点、透明度、表示状態を元PNGの`waDAimage`メタデータへCRC付きで書き戻し、MIF保存後の
  再読込でも編集結果を維持するようにした。選択・移動・変形・回転とMIF往復の自動テストを追加した。
- 画像レイヤーの基準X／Y、回転軸に沿った幅／高さ、不透明度をオブジェクト設定へ接続した。
  数値編集でも回転と反転方向を維持して4頂点を更新し、Undo／Redoできる。
- 画像レイヤーを内部JSONへPNGのBase64と4頂点で可逆保存し、SVGへ標準`image`要素、埋め込みPNG、
  アフィン`matrix`として保存・読込するようにした。標準的な未変形SVG画像の取込みもテストした。
- `四角_回転2.mif`を基準に、新規Rectangleのvector変換行列が丸め前の回転頂点を使用し、全4基準頂点を
  整数のimage配置4頂点へ正しく写すことを確認した。補助vector IPNGもWebArt Designer 7の76x1データと
  バイト単位で一致しており、両条件をMIF往復テストへ固定した。
- 複数選択した画像を共通外接枠から移動・拡大縮小できるようにし、各画像の回転・反転を保つ4頂点変換を
  1回のUndo／Redoへまとめた。同種の複数画像はPNG本体と属性を保持したまま24pxずらして複製でき、
  複製一式のUndo／Redoと選択復元にも対応した。
- 外部ファイルを参照するSVG `image`は文書の可搬性とオフライン動作を損なうため現段階では対象外とし、
  `data:image/png;base64`で自己完結する標準image要素だけを読み込む方針とした。
- ツールパレットへPathツールを追加した。クリックで頂点を追加し、先頭頂点の再クリックで閉じたPath、
  ダブルクリックまたは右クリックで開いたPathを確定する。作成結果は1回のUndo／Redo対象にした。
- 単一選択したPathの全頂点へ編集ハンドルを表示し、各頂点をキャンバス内で直接ドラッグできるようにした。
  頂点操作はPath全体の移動と別の履歴操作としてUndo／Redoできる。
- Object Propertiesの外接X／Y／幅／高さ、塗り色、線色、線幅、線種、不透明度をPathへ接続した。
  外接寸法の変更では各頂点の相対位置を保って拡大縮小する。
- Path作成、頂点編集、Undo／Redoの自動テストを追加し、JSON／SVG／MIF往復テストを再実行した。
  単独アプリと画面レイアウトプラグインのDebug／Release Win64ビルドも警告0、エラー0で成功した。

## 2026-08-26

- Rectangleへ中心回りの回転角を追加し、共通Geometryで回転4頂点、外接範囲、逆変換ヒットテストを
  計算するようにした。Skia共通レンダラー、JSON、SVGの`rotate`変換へ同じ回転状態を接続した。
- `四角_回転.mif`を解析し、回転後の`image position1..4`、未回転の`vector original position1..4`、
  `vector matrix a..f`の対応を確認した。MIF読込では4頂点から中心、寸法、角度を復元し、保存では
  回転4頂点とアフィン行列を生成するようにした。
- 単一選択したRectangleの回転後四辺へ選択枠を合わせ、4頂点の外側へ回転マーカーを配置した。
  マーカーのドラッグは図形中心を基準とし、1回のUndo／Redo対象にした。回転後のリサイズも
  図形のローカル軸で処理し、反対側の辺または頂点を固定する。
- JSON、SVG、MIF、Skia描画、提供MIF読込、回転操作とUndo／Redoの自動テストが成功した。
  単独アプリと画面レイアウトプラグインのDebug／Release Win64ビルドも警告0、エラー0で成功した。
- 回転マーカーへホバーした際、コード生成した円弧矢印の専用カーソルを表示するようにした。
- キャンバス上の選択オブジェクト、またはフォーカス中のレイヤー一覧でDeleteキーを押すと、
  操作バーと同じ複数削除を実行するようにした。削除はUndo／Redo対象とし、ロック選択では実行しない。
- 選択オブジェクトの削除中に隣接レイヤーが自動選択され、選択外まで連続削除される問題を修正した。
  操作開始時の選択を固定し、単一選択と飛び飛びの複数選択で選択外が残ることをテストした。
- 「開く」ダイアログの先頭へMIF／SVG共通フィルターを追加し、両形式を同じ一覧から選べるようにした。
- Rectangleの塗りと線を分離し、線色、線幅（0で線なし）、実線／破線をDocument、作成時既定値、
  Undo／Redo、オブジェクト設定、Skia描画、レイヤーサムネイルへ接続した。
- 線スタイルをJSON、SVG、MIFで往復可能にした。MIF実サンプルから実線style 0、点線系style 3、
  倍精度の線幅、ストロークtextureの線色を読み込み、自前生成MIFにも同じメタデータと画像を出力する。
- 親Frameがウィンドウへ接続される前に線種ComboBoxのItemsがHandle生成を要求し、起動時例外になる問題を
  修正した。選択肢と選択値をComboBoxのCreateWndまで遅延し、Debug／Releaseの実起動を確認した。
- 元アプリの線種一覧に合わせ、MIF `vector stroke style` 0～8を9種類の線パターンとして保持するようにした。
  ComboBoxも同じ順序の線見本を描画し、Skia、SVG、JSON、MIFで各パターンを共通化した。
- 回転Rectangleの選択枠を中心からの放射状拡張ではなく、各辺から均等に外側へ離す計算へ変更した。
  基本間隔8pxに表示線幅の半分を加え、ズーム時の太線にも重ならないよう描画と当たり判定を共通化した。
- 選択済みRectangleの本体を再クリックすると、回転追従枠と水平・垂直な外接枠を切り替えるようにした。
  外接枠リサイズは回転角と長方形を維持して幅・高さへ換算し、45度付近では縦横比を保って安定させる。
  本体ドラッグは選択方式を切り替えず従来どおり移動し、別オブジェクト選択時は回転追従枠へ戻る。
- 選択枠方式の再クリック切替後にDocument更新がなく再描画されない問題を修正し、マウスアップで必ず
  キャンバスを再描画するようにした。クリックと移動ドラッグの判定幅も6pxへ広げた。
- Rectangleに続くオブジェクトとして直線レイヤーを追加した。ツールパレットからのドラッグ作成、
  Skia描画、線本体の選択と移動、両端点のドラッグ、削除、Undo／Redo、レイヤーサムネイル、線色・線幅・
  9線種の設定を接続した。
- 直線をJSONの`line`、SVGの標準`line`、MIFの`vector element type=6`として読み書きするようにした。
  元アプリ製`線.mif`から端点、100px線幅、style 8を復元し、自前MIFの基本往復も確認した。
- 直線の選択表示を両端の四角ハンドル2個だけに変更し、選択枠や線上への重ね描きを廃止した。
  ハンドルは端点から画面上6px空けて外側へ配置し、ズーム時も間隔を保ったまま端点編集できる。
- 元アプリ製の連続直線と多角形が直線と同じ`vector element type=6`を使用し、vector PNG内に
  60バイト単位の頂点コマンド列を持つことを確認した。Pathレイヤーへ複数頂点、開閉状態、塗りと線を保持し、
  実サンプルの読込、Skia描画、本体選択、移動、削除とUndo／Redoへ接続した。
- Pathの頂点コマンド列を持つvector PNGを新規生成し、MIF保存後も頂点、開閉状態、塗り、線を
  再読込できるようにした。SVGは標準`polyline`／`polygon`、内部JSONは頂点配列として可逆保存する。

## 2026-08-25

- MIF Reader／Writerを全サンプルへ一括適用する診断ツールを追加した。往復保存、バイト差分、SHA-256、
  MIF／PNGチャンク、CRC、`tEXt`／`waDA`、JSON集計、任意の完全16進ダンプをAIから1コマンドで生成できる。
- Rectangle Documentから、合成プレビューPNG、背景texture、imageオブジェクトを持つMIFを新規生成する
  保存処理を追加した。配置、透明度、表示状態を`waDAimage`へ、再編集用JSONをPNGの`tEXt`へ記録する。
  自身が保存したMIFをDocumentへ戻す試験、PNG CRC診断、Debug／Releaseビルドがすべて成功した。
- MIFは未知チャンクを完全維持する編集形式とはせず、対応内容をDocumentへ読み込んだ後の保存では
  未対応オブジェクトや未知メタデータが失われることを許容する方針とした。
- Debug版でMIFを開いた際、元ファイルの隣へ外側チャンク、PNG寸法・CRC、`tEXt`／`waDA`、
  Document変換結果を記録する`.open.log`を出力するようにした。別アプリ製の四角MIFでログ内容を検証した。
- 別アプリ製MIFの`object subtype=vector`かつ`element type=4`をRectangleへ変換し、キャンバス、
  包含座標、塗りtexture、透明度、hiddenを読み込むようにした。保存側もimage、塗りtexture、
  vector IPNG、線textureの8外側チャンク構成へ変更し、vectorメタデータと変換行列を生成する。
- 生成PNGへ`pHYs`を追加し、`tEXt`と`waDA`をすべて`IDAT`より前へ配置して別アプリの並びへ合わせた。
- AviUtl2上のフィルター名と編集画面タイトルを、記号付きの`【画面レイアウト】`から
  `画面レイアウト`へ修正した。
- Syncroh2のフィルター実装を参考に、AviUtl2のObject IDごとに所有権付きコンテキストを
  取得・生成する`PluginFilterContextManager`を共通`Lib\AviUtl2`へ追加した。
- 画面レイアウト用コンテキストに`配置データ`と解析済みDocumentを保持し、
  変更時だけ再解析する映像コールバックの基盤を接続した。
- 同一Object IDの再利用、異なるObject ID間の分離、不正JSONの非破壊性を単体テストし、
  画面レイアウトプラグインのDebug／Release Win64ビルドが警告0、エラー0で成功した。
- コンテキストのキーをObject IDとEffect IDの組み合わせへ拡張し、同一オブジェクト内の
  異なるフィルター実体も分離できるようにした。
- `SYNC_Lyrics`のフレームバッファ取得を参考に、`GetFramebufferTexture2D`から合成済みの
  AviUtl2現在映像を毎フレーム取得し、オブジェクト別コンテキストへ保持する処理を追加した。
- 選択オブジェクトの`編集`ボタンから、対応コンテキストの最新背景を共通キャンバスへ渡し、
  Direct2DとGDIで参照表示するようにした。背景はDocumentやJSONに保存せず、単独アプリは従来どおり
  背景未設定で動作する。
- 画面レイアウトプラグインと単独アプリのDebug／Release Win64ビルドを行い、すべて
  警告0、エラー0を確認した。
- 新規Documentのサンプル四角形2個を廃止し、単独アプリとプラグイン編集画面をCanvasだけの
  空状態から開始するようにした。AviUtl2の`配置データ`文字列を画面表示前にJSONから復元し、
  画面終了時にJSONへ変換してUTF-8で書き戻す経路と、空Documentの往復を検証した。
- Documentの図形をSkia raster-directで透明RGBA8へ描く共通レンダラーを追加し、Delphi編集画面と
  AviUtl2映像出力の四角形描画を同じ実装へ統合した。AviUtl2側は入力映像へSource-over合成し、
  Document Revision単位で描画結果をキャッシュする。透明背景、色、透明度、合成結果をピクセルテストした。
- Canvasを共通レイヤー一覧の表示・当たり判定から除外し、単独アプリの`編集`メニューへ
  `キャンバスの設定`を追加した。現在値を初期選択する解像度プリセット専用ダイアログから
  Documentを更新し、プラグイン編集画面では同メニュー項目を非表示にした。ダイアログは
  ダーク表示へ統一し、解像度のダブルクリックでもOKと同じ確定操作を行えるようにした。
  標準VCLボタンは使用せず、OKを青、Cancelを濃いグレーとする専用ダークボタンへ置き換え、
  ホバー、押下、フォーカス、Enter／Escの各操作状態も実装した。
- 独自のFile／編集／表示メニューを一括管理し、フォームが非アクティブになった場合、または
  メニュー外の画面をクリックした場合にポップアップを閉じるようにした。別メニューを開く際も
  先に開いていたポップアップを閉じ、複数のメニューが残らないようにした。いずれかのメニューが
  開いている間は、隣のトップメニューへマウスを移動するだけでサブメニューを切り替える。
- 独自メニューのControl生成、項目追加、開閉、所有Control判定を`Lib/DarkMenu`の
  `TVectArtDarkPopupMenu`へ移し、複数メニューの相互切替、外側クリック、非アクティブ監視も
  `TVectArtDarkMenuGroup`へ共通化した。新規ControlとDFM上の既存Controlの両方へ接続できる。
- 単独アプリの`ファイル`メニューを日本語化し、`開く`、`上書き保存`、`名前を付けて保存`を
  MIF専用のファイルダイアログへ接続した。新規状態でも名前を付けて保存は選択でき、上書き保存は
  保存先が確定するまで無効とする。プラグイン編集画面ではアプリ専用Fileメニューを表示しない。

## 2026-08-21

- 作業ノートを3プロジェクト共通の`note.md`と、単独編集アプリ固有の
  `note_VectArtDesigner.md`へ分割した。
- ルートへ`SYNC_ScreenLayout_Filter`プロジェクトを追加し、AviUtl2上の`画面レイアウト`、
  分類`SYNC`、`編集`ボタン、`配置データ`文字列だけを持つ最小フィルタープラグインを作成した。
- Syncroh2の`PluginFilterTable.pas`を共通`Lib\AviUtl2`へ移植し、画面レイアウトプラグインの
  テーブル設定とGUI項目登録を共通ヘルパー経由へ変更した。
- 画面レイアウトプラグインのWin64ビルド後にAviUtl2の専用プラグインフォルダへ配置し、
  `.auf2`への改名後に中間の`.dll`、`.rsm`、`.exe`を削除するビルドイベントを追加した。
- `編集`ボタンから単独アプリと同じ編集画面を開き、AviUtl2の`配置データ`をDocument JSONとして
  読み込み、画面終了時にUTF-8で書き戻すプラグイン用ホストを追加した。
- Document JSONバージョン1の往復テストを追加し、日本語レイヤー名、矩形、透明度、ロック、
  不正JSON時の非破壊性を確認した。
- 共通UIをDLLから使う場合もモジュール自身と同じ場所の`sk4d.dll`を参照するようSkia初期化を
  共通化し、プラグインのビルド後配置へSkiaランタイムのコピーを追加した。

## 2026-08-20

- プロジェクト用の `note.md` を作成した。
- 基本的なビルドルールとコメントルールを整理した。
- 制作履歴を本ファイルへ分離し、`note.md` が履歴で肥大化しない構成にした。
- `WRT2646\Client` のVCLプロジェクト設定を基に、`VectArtDesigner` プロジェクトを作成した。
- WRT2646固有の通信処理と画面部品は持ち込まず、メインフォーム1つだけの最小構成にした。
- Delphi 37.0でDebug／ReleaseのWin64ビルドを行い、警告0、エラー0を確認した。
- Git設定を確認し、MIFをバイナリー同期対象として `.gitattributes` に登録した。
- 誤って追跡されていたビルド成果物の `.rsm` を同期対象から外す設定にした。
- ダーク表示のメインフォームへ、レイヤー、固定編集ツール、中央編集、オブジェクト設定の
  4つの最小Frameを配置する初期ドッキング骨格を追加した。
- 実行画面を取得し、ダークタイトルバー、固定メニュー・ショートカット・ステータス領域と、
  4つのFrameが起動時に想定位置へ配置されることを確認した。
- `表示`メニューへチェック付きの`レイアウト編集`を追加し、編集モードで3つのツールFrameへ
  移動チップを表示するようにした。
- Layersの移動チップを使い、フローティングウィンドウへの分離と既定ドックへの再結合を確認した。
- 固定ホスト方式を専用ドック管理クラスへ置き換え、分離時に元の空スロットを削除して
  中央編集領域を自動拡張するようにした。
- Layersを左側から右側へ移動し、Object Propertiesと同じ側へ複数列で結合できることを確認した。
- 右側でLayersをObject Propertiesの前後へ入れ替え、ドロップ位置による列順変更を確認した。
- ドラッグ中の左右端ドッキング候補表示と、編集モード終了時のフローティングツール復帰を追加した。
- 分離中の専用移動チップを非表示にし、フローティングフォームのタイトルバー操作で
  移動と再ドッキングができるようにした。
- `表示`メニューへLayers、Tools、Object Propertiesのチェック項目を追加し、ツールの
  表示・非表示と最後のドック位置への再表示を実装した。
- レイアウト編集中の右クリックにダーク表示の`閉じる`メニューを追加した。
- `Documents\VectArtDesigner\MainForm.ini`へメインフォーム位置、ツールの表示状態、左右の
  順序、ドック／フローティング状態と座標を保存し、起動時に復元する処理を追加した。
- 保存座標が現在のモニター範囲外の場合、最寄りモニターの作業領域へ補正するようにした。
- 中央編集FrameをDirect2D優先の編集キャンバスへ置き換え、1920×1080の白い論理キャンバスを
  編集領域へ収まる倍率で中央表示するようにした。
- Syncroh2で使用しているGoogle Skia対応TextRendererを`Lib/TextRenderer`へコピーした。
- Win64用`sk4d.dll`を`Lib/Skia/Win64`へ配置し、ビルド後の実行フォルダーへのコピーと、
  アプリケーション起動・終了時のSkiaランタイム取得・解放を追加した。
- Delphi 37の動的Skia APIを`SkInitialize`／`SkFinalize`で明示的に管理し、起動時に
  `TSkGraphics.Init`が未設定の関数ポインタを呼び出す実行時エラーを修正した。
- キャンバス生成途中は`ClientWidth`／`ClientHeight`を参照せず、VCLコントロールの
  ハンドル作成前に`HandleNeeded`が呼ばれる起動時エラーを修正した。
- 編集キャンバスへカーソル位置基準のホイール拡大縮小（フィット比の0.25～8倍）と、
  マウスキャプチャを使った右ドラッグ移動を追加した。左ドラッグは範囲選択用に予約した。
- Direct2D優先のレイヤー一覧を追加し、配列先頭を最背面として一覧下端から上へ積む
  表示構造と、1920×1080の白い初期キャンバスの16:9サムネイルを実装した。
- 用紙とオブジェクトを共有するDocumentモデルを追加し、サイズ・背景色・透明設定を持つ
  Canvasレイヤーと、中央配置した480×480の白いRectangleレイヤーを初期状態にした。
- 編集画面とレイヤー一覧をDocumentへ接続し、Rectangleの描画・選択輪郭・配置を反映した
  サムネイル、および一覧クリックによる選択切り替えを実装した。
- オブジェクトのレイヤーサムネイルを用紙全体基準からオブジェクト外接サイズ基準へ変更し、
  一覧要素の高さへアスペクト比を保って拡大するとともに、市松模様を細分化した。
- 別位置・別色のRectangleを初期サンプルへ追加し、キャンバスの左クリックによる選択切替と、
  図形から離した選択枠および四隅・各辺中央の8個の選択マーカーを実装した。
- Direct2D／GDIの`FrameRect`がBrushを使う仕様に合わせ、選択枠を黒線、8個のマーカーを
  白塗り・黒枠で確実に描画するよう修正した。
- コード肥大化を避ける構成方針を`note.md`へ追加し、既存ユニットを`Core`、`Editor`、
  `Layers`、`Layout`、`ObjectProperties`、`ToolPalette`、`Shell`へ再配置した。
- 4ツールをまとめていたWorkspaceFramesを、共通ドッキングFrameとEditor、Layers、
  Object Properties、Tool PaletteそれぞれのFrameユニットへ分割した。
- 選択枠の幾何計算とキャンバス入力状態をEditor配下の独立ユニットへ分離し、図形内部の
  左ドラッグ移動と8個の選択マーカーによる8方向リサイズを実装した。
- Rectangleの座標変更をDocumentの専用メソッド経由にし、ドラッグ中の編集画面と
  レイヤーサムネイルを同じ変更通知で更新するようにした。
- 空白からの左ドラッグで選択矩形を表示し、範囲と交差する複数のRectangleを選択する
  処理を追加した。主選択には8マーカー、追加選択には外枠を表示するようにした。
- Documentへ複数レイヤーの選択集合を追加し、キャンバスとレイヤー一覧の複数選択表示を
  同じ状態から更新するようにした。
- 選択済み図形をドラッグしたときに複数選択を維持し、選択中の全Rectangleへ同じ移動量を
  適用するグループ移動へ対応した。
- 複数選択時の個別枠を、全選択Rectangleの配置を包む1つの外接選択枠と8マーカーへ変更した。
  外接枠のマーカー操作では、選択図形の相対配置を保ったまま全体を拡大縮小する。
- Object Propertiesへ選択RectangleのX、Y、幅、高さ、塗り色を接続し、単一選択では数値と
  `#RRGGBB`入力による編集を即時反映するようにした。複数選択では外接範囲を読み取り専用表示する。
- CoreへUndo／Redo履歴と座標・塗り色の編集コマンドを分離し、キャンバスの移動・単一／複数
  拡大縮小とObject Propertiesの編集を履歴へ接続した。ドラッグ中の連続更新は1操作にまとめる。
- `Ctrl+Z`でUndo、`Ctrl+Y`または`Ctrl+Shift+Z`でRedoを実行し、ショートカット表示へ
  各操作の使用可否を反映するようにした。
- レイヤー一覧へコード描画の表示・ロックボタンを追加し、Canvasを含む各レイヤーの表示状態と
  Rectangleの編集ロックを切り替えられるようにした。状態変更はUndo／Redo対象とする。
- Object Propertiesへ不透明度（0～100%）を追加し、Direct2D描画とレイヤーサムネイルへ
  反映した。ロック選択は外接枠のみ表示し、移動・拡大縮小・座標・色編集を抑止する。
- レイヤーの表示・ロックアイコンをサムネイル右上から左側の専用列へ移し、目を上、鍵を下に
  縦配置した。サムネイルと名称表示はアイコン列を避けて右へ配置する。
- 複数選択時のObject Propertiesを編集可能にし、外接範囲のX、Y、幅、高さから相対配置を
  保った一括変換を行うようにした。塗り色と不透明度も一括適用し、混在値は空欄表示する。
- 複数レイヤーへの一括変更を1回のUndo／Redoとして扱う複合編集コマンドを追加した。
- EditメニューへUndo／Redo項目を追加し、ショートカットバーをコード描画アイコンへ変更した。
  履歴の有無に応じてメニューとUndo／Redoボタンの有効表示を更新し、クリック操作へ接続した。
- 編集履歴ユニットから各コマンド型を`Source\Core\Commands`へ分離し、履歴ユニットはコマンド列と
  Undo／Redo位置の管理だけを担当する構成へ整理した。新しい編集UIもMainFormとは別ユニットにした。
- レイヤー一覧下部へコード描画の追加、複製、削除、前面移動、背面移動ボタンを追加した。
  Canvasレイヤーとロックレイヤーでは適用できない操作を無効表示する。
- Rectangleの挿入、削除、積層順変更をDocumentの構造APIへ分離し、各操作の選択状態を含めて
  Undo／Redoする専用コマンドを`Source\Core\Commands`へ追加した。
- レイヤー操作バーを`VectArtDesignerLayerActions`へ分離し、既存のレイヤー一覧描画Controlへ
  構造変更の入力処理を追加しない構成とした。
- ツールパレットへコード描画の選択ツールとRectangleツールを追加し、共有EditorStateから
  選択状態、キャンバスカーソル、ステータス表示を同期するようにした。
- Rectangleツールで用紙内を左ドラッグすると外枠をプレビューし、確定時に任意サイズの
  Rectangleを最前面へ追加する処理を実装した。作成操作はUndo／Redo対象とする。
- 図形作成の入力状態と確定処理を`VectArtDesignerShapeCreation`へ分離し、キャンバスControlには
  ツール別入力の振り分けとプレビュー描画だけを追加した。
- レイヤー一覧のサムネイル、市松模様、不透明度、状態アイコン、行配置を専用の
  `VectArtDesignerLayerRenderer`へ移し、Direct2D／GDI描画を一覧Controlから分離した。
- レイヤー一覧Controlを描画方式の切替、クリック判定、Document接続だけに縮小し、描画と
  クリック入力がRendererの同じ行・アイコン配置計算を使用する構成にした。
- 複数選択したRectangleをまとめて削除し、離れた選択を含む各レイヤーのまとまりを相対順を
  保ったまま1段前面／背面へ移動できるようにした。各操作は1回のUndo／Redoとして扱う。
- ロックレイヤーを含む複数選択では、削除・複製・積層移動を無効にした。
- レイヤー構造操作の判定と実行を`VectArtDesignerLayerOperations`へ分離し、操作バーControlは
  ボタン描画と入力転送だけを担当する構成へ縮小した。
- Rectangle作成中にShiftで正方形、Altで開始点を中心とする矩形、Shift+Altで中心基準の
  正方形を作成できるようにした。プレビューと確定結果に同じ補正を適用し、用紙境界内へ収める。
- キャンバスにフォーカスがあるとき、選択Rectangleを矢印キーで1px、Shift+矢印キーで10px
  移動できるようにした。複数選択は相対配置を維持し、各キー入力を1回のUndo／Redo対象とする。
- キーボード移動処理を`VectArtDesignerKeyboardMovement`へ分離し、オブジェクト設定などの
  入力欄にフォーカスがある場合は矢印キーを編集画面側で処理しないようにした。
- 選択した複数Rectangleを相対配置と積層順を維持して24px右下へ複製し、複製一式を
  最前面へ追加して選択するようにした。重複しないCopy名を割り当て、1回のUndo／Redo対象とする。
- 複製データ生成を`VectArtDesignerLayerDuplication`、一括挿入履歴を
  `VectArtDesignerLayerBatchCommands`へ分離し、既存のレイヤー操作ユニットの肥大化を避けた。
- Rectangleツールの共有スタイルとして塗り色と不透明度をEditorStateへ追加した。初期値は
  既定色・不透明度100%とし、Object Propertiesで確定した値を次のRectangle作成へ継承する。
- キャンバスのドラッグ作成とレイヤー操作バーの追加ボタンを同じ共有スタイルへ接続した。
  複製操作は共有スタイルではなく、従来どおり複製元レイヤーのスタイルを保持する。
- デザイナー用の4つのFrameへ個別に渡していたDocument、編集履歴、EditorStateを、共通の
  `IVectArtDesignerContext`へ集約した。標準アダプターはサービスを所有せず、別アプリからも
  同じContext接続口でFrame群を再利用できる構成にした。
- MIF外側コンテナーのReader／Writerを追加し、MIMGシグネチャ、ビッグエンディアン長、MHDR、
  IPNG、MENDを境界検査付きで読み書きできるようにした。未知ブロックは解釈せずそのまま保持する。
- MIFの読込と保存を`IVectArtMifContainerReader`／`IVectArtMifContainerWriter`へ分離し、具象クラスを
  factoryの背後へ隠した。Containerはデータ保持だけを担当し、Streamとファイルの入出力を差し替え可能にした。
- FileメニューへOpen、Save、Save Asを追加し、標準ファイルダイアログからMIF Reader／Writerを
  呼び出せるようにした。ツールバーのOpen／SaveとCtrl+O／Ctrl+S／Ctrl+Shift+Sも同じ処理へ接続した。
- MIF読込後はファイル名とチャンク数を表示し、Documentへの画面展開が未実装であることをステータスへ
  明示するようにした。保存は同一フォルダーの一時ファイルへ書いた後で置き換える。
- 調査用MIF全12ファイルをDelphi製の試験プログラムで読み込み、メモリへ再保存した結果、全ファイルで
  元データと1バイト単位で完全一致することを確認した。
- PNG内の`waDA*`情報は通常の`tEXt`キーではなく、チャンクtypeを`waDA`、dataをキー接尾辞、NUL、
  値とする独自PNGチャンクであることを確認した。

## 2026-08-19

- IBM WebArt DesignerのMIF形式について、コンテナー構造、埋め込みPNG、主要メタデータを調査した。
- 詳細な解析結果を [mif_analysis.md](mif_analysis.md) に記録した。

## 初期検討仕様の保管（2026-09-08統合）

これは旧方針を含む検討履歴であり、現在の実装指示ではない。現在方針はnote.md、引き継ぐ追加候補はnote_VectArtDesigner.mdを正とする。元ファイル削除に伴い、初期検討の原文を見出し階層のみ変更して保管する。

### VectArtDesigner 実装前検討仕様

#### 1. 目的

> この文書は初期検討の履歴であり、AviUtl2連携やMIFに制限されない編集方針は廃止済みである。
> 現在方針は`note.md`と`note_VectArtDesigner.md`を参照する。

VectArtDesigner は、サムネイルなどの静止画デザインを効率よく作成するためのベクター系デザインアプリとする。

主な目的は以下の3点。

1. Webアートデザイナの MIF ファイルを読み込み、既存データを救済・再利用できるようにする。
2. SVG 系の描画・保存機構をフレームワークとして利用し、VectArtDesigner 独自の編集機能を構築する。
3. AviUtl2 プラグインから編集・描画機能を利用できるようにし、動画編集ソフト上で直接画像デザインを扱えるようにする。

VectArtDesigner は MIF エディタでも完全な SVG エディタでもない。
VectArtDesigner 独自の内部データモデルを正本とし、MIF と SVG は入出力・互換のための形式として扱う。

---

#### 2. 対応形式の基本方針

##### 2.1 MIF

MIF は既存データ救済を主目的とする。

- 読み込み互換性を重視する。
- MIF の各要素を VectArtDesigner の内部オブジェクトへ変換する。
- MIF 固有情報を内部で可能な限り再現する。
- 再現不能な要素がある場合の扱いを決める。
- MIF 書き出しは重視しない。
- 必要であれば最低限の書き出しのみ対応する。
- MIF を読み込んだ後は、基本的に VectArtDesigner のデータとして編集する。

##### 2.2 SVG

SVG は完全互換を目的としない。

- SVG の仕組みを描画・保存フレームワークとして利用する。
- VectArtDesigner が正式対応する SVG 要素だけを読み込む。
- 非対応要素を無理に完全再現しない。
- VectArtDesigner から書き出した SVG は再編集可能であることを重視する。
- 一般的な SVG ビューアでも可能な範囲で表示できる形式とする。
- VectArtDesigner 固有情報を SVG 内へ保持する方法を検討する。

##### 2.3 独自情報

SVG だけでは表現しづらい機能を今後追加する可能性がある。

例：

- 複数縁取り
- 特殊な影
- 立体文字
- サムネイル専用装飾
- AviUtl2 用情報
- 編集用ガイド情報
- プリセット参照情報

これらを SVG 内の独自メタデータとして保存するか、別形式を併用するかを決める。

---

#### 3. VectArtDesigner 内部データモデル

実装開始前に最優先で決定する。

##### 3.1 基本オブジェクト種類

候補：

- Line
- Shape
- Path
- Text
- Image
- Group

Shape の派生として以下を扱うか検討する。

- Rectangle
- Rounded Rectangle
- Ellipse
- Circle
- Polygon
- Star
- その他定型図形

##### 3.2 オブジェクト共通情報

全オブジェクトが持つべき情報を決める。

候補：

- ID
- 名称
- 表示 / 非表示
- ロック
- X / Y
- Width / Height
- 回転
- 拡大率
- 基準点
- 透明度
- Transform
- 親 Group
- 描画順
- 選択状態はデータとして保持するか GUI 状態とするか

##### 3.3 座標系

以下を確定する。

- 原点位置
- Y 軸方向
- 内部座標の単位
- 小数座標を許可するか
- SVG 座標系との変換
- MIF 座標系との変換
- AviUtl2 座標系との変換
- キャンバス中心基準と左上基準のどちらを内部基準にするか

##### 3.4 変形

以下の扱いを決める。

- 移動
- 拡大縮小
- 回転
- 左右反転
- 上下反転
- スキュー
- Matrix を直接保持するか
- Shape / Text / Image の Width・Height を変更する方式と Transform 方式の使い分け

---

#### 4. レイヤー・グループ構造

##### 4.1 レイヤー

現在の左ペインを基本とする。

検討項目：

- 描画順
- 表示 / 非表示
- ロック
- 透明度
- 名称変更
- 複数選択
- 並び替え
- 複製
- 削除
- サムネイル生成

##### 4.2 グループ

以下を決める。

- Group を通常オブジェクトとして持つか
- ネストを許可するか
- グループ全体 Transform
- グループ全体透明度
- グループ内クリッピング
- グループ展開 / 折りたたみ
- グループ単位 Undo

---

#### 5. 描画属性

##### 5.1 Fill

対応候補：

- 単色
- 透明
- 線形グラデーション
- 放射グラデーション
- テクスチャ

##### 5.2 Stroke

対応候補：

- 色
- 太さ
- 透明度
- 線種
- 線端
- 結合部
- 内側 / 中央 / 外側

MIF と SVG で仕様差がある部分の変換方法を決める。

##### 5.3 Shadow

検討項目：

- ON / OFF
- X / Y
- 色
- 透明度
- ぼかし
- 拡張量
- 複数影を許可するか
- 立体表現への流用

##### 5.4 Opacity

- オブジェクト全体透明度
- Fill 透明度
- Stroke 透明度
- Shadow 透明度
- グループ透明度

これらを別管理するか決める。

---

#### 6. Path / ベジェ曲線

##### 6.1 Path 基本仕様

- 開いた Path
- 閉じた Path
- 直線
- 二次ベジェ
- 三次ベジェ
- 複数サブパス

##### 6.2 編集

- 頂点追加
- 頂点削除
- 頂点移動
- ハンドル編集
- スムーズ / コーナー切替
- Path 結合
- Path 分割
- 開閉切替

##### 6.3 ブーリアン

サムネイル・ロゴ作成を考慮して必要性を検討する。

- 合成
- 減算
- 交差
- 排他

---

#### 7. Text

サムネイル用途では最重要機能の一つ。

##### 7.1 基本

- 文字列
- フォント
- サイズ
- 太字
- 斜体
- 下線
- 取消線
- 字間
- 行間
- 横書き / 縦書き
- 左 / 中央 / 右揃え

##### 7.2 装飾

- Fill
- Stroke
- 複数縁取り
- 影
- グラデーション
- テクスチャ
- 立体表現
- 凹み表現

##### 7.3 文字の図形化

- Text → Path 変換
- 変換後は通常 Path として編集
- 元文字情報を残すか
- コピーして立体や影を作る操作との関係

---

#### 8. Image

##### 8.1 対応画像

候補：

- PNG
- JPEG
- GIF
- BMP
- WebP

##### 8.2 保持方法

- 外部ファイル参照
- 埋め込み
- SVG data URI
- 独自リソース管理

AviUtl2 との受け渡しを考慮して決める。

##### 8.3 編集

- 移動
- サイズ変更
- 回転
- 反転
- 透明度
- トリミング
- マスク

---

#### 9. サムネイル作成向け機能

一般 SVG エディタとの差別化部分。

##### 9.1 配置

- 左右中央揃え
- 上下中央揃え
- 等間隔
- キャンバス基準整列
- 選択範囲基準整列
- スナップ
- ガイド
- グリッド

##### 9.2 文字レイアウト

- タイトル向けプリセット
- 縁取りプリセット
- 影プリセット
- 立体文字プリセット
- 文字サイズの高速調整
- 行単位調整

##### 9.3 色

- カラーパレット
- 最近使用した色
- プロジェクト色
- スポイト
- 補色等の支援を行うか

##### 9.4 テンプレート / プリセット

- オブジェクトスタイル
- 文字スタイル
- 配色
- 図形装飾
- レイアウト
- サムネイルサイズ

---

#### 10. GUI 基本構成

現状の構成を基本とする。

##### 10.1 上部

- ファイル
- 編集
- 表示
- New
- Open
- Save
- Undo
- Redo

##### 10.2 左

- レイヤーリスト

##### 10.3 中央左

- 選択
- Shape
- Line
- Path
- Text
- Image
- その他ツール

##### 10.4 中央

- キャンバス

##### 10.5 右

選択オブジェクトのプロパティ。

将来的には以下の分類を検討する。

- Transform
- Appearance
- Type Specific

##### 10.6 下部

- 現在ツール
- キャンバスサイズ
- ズーム
- カーソル座標
- 状態表示

---

#### 11. 選択・編集操作

実装前にマウス操作を統一する。

検討項目：

- 単体選択
- Ctrl 複数選択
- Shift 範囲選択
- 選択解除
- ドラッグ移動
- ハンドルサイズ変更
- 回転ハンドル
- Alt / Shift / Ctrl を使った補助操作
- キャンバスパン
- ホイールズーム
- 頂点編集モード
- オブジェクト編集と Path 編集の切替

---

#### 12. Undo / Redo

早い段階で設計しておく。

対象候補：

- オブジェクト追加
- 削除
- 移動
- サイズ変更
- 回転
- 属性変更
- レイヤー移動
- Group 化
- Path 頂点編集
- Text 編集

連続ドラッグを Undo 1 回として扱うなど、操作単位を決める。

---

#### 13. コピー・貼り付け

検討項目：

- アプリ内部コピー形式
- 複数オブジェクト
- Group
- Image リソース
- Text フォント情報
- 他アプリへの SVG コピー
- 画像としてのコピー

---

#### 14. MIF 読み込み仕様

既存 MIF 解析結果をもとに正式対応表を作る。

各 MIF 要素について以下を記録する。

- MIF 種類
- 内部オブジェクトへの変換先
- 完全再現可能
- 近似再現
- 非対応
- 読み込み時警告の必要性

対象候補：

- 四角
- 線
- 連続線
- 曲線
- 自由曲線
- 文字
- 画像
- 塗り
- 縁
- 影
- グラデーション
- テクスチャ
- 回転
- 透明度
- 各種文字効果

---

#### 15. SVG 対応範囲

VectArtDesigner が正式サポートする SVG サブセットを決める。

初期候補：

- svg
- g
- rect
- circle
- ellipse
- line
- polyline
- polygon
- path
- text
- image
- fill
- stroke
- opacity
- transform
- linearGradient
- radialGradient

対応外候補：

- script
- CSS 完全対応
- animation
- filter 完全対応
- 外部 SVG 参照
- 複雑な mask
- 複雑な clipPath

非対応 SVG を読み込んだ場合の処理を決める。

- 無視
- 警告
- 可能な範囲で近似
- ラスタライズ

---

#### 16. 保存仕様

##### 16.1 通常保存

候補：

- SVG をプロジェクト兼保存形式とする
- SVG + 独自メタデータ
- 独自プロジェクト形式 + SVG 出力

##### 16.2 自動保存

- 自動保存周期
- クラッシュ復旧
- 一時ファイル

##### 16.3 外部リソース

- 画像
- フォント
- テクスチャ

ファイル移動時に壊れない管理方法を決める。

---

#### 17. 描画エンジン

GUI と描画処理を分離する。

同一の描画エンジンを以下から利用できることが望ましい。

- VectArtDesigner 本体
- レイヤーサムネイル
- ファイル書き出し
- AviUtl2 プラグイン

検討項目：

- CPU / GPU
- アンチエイリアス
- 透明合成
- グラデーション
- Text
- Image
- Path
- キャッシュ
- 高解像度描画

---

#### 18. AviUtl2 連携

VectArtDesigner の重要機能。

##### 18.1 役割分担

VectArtDesigner：

- 編集 GUI
- レイヤー編集
- オブジェクト生成
- デザインデータ作成

AviUtl2 プラグイン：

- データ保持または参照
- 描画要求
- AviUtl2 フレームへの合成
- VectArtDesigner 編集 GUI の起動

##### 18.2 データ受け渡し

候補：

- シリアライズデータ
- 共有メモリ
- ファイル
- DLL 呼び出し

既存の「編集側へシリアライズデータを渡し、編集後のデータを返す」方式との統一を検討する。

##### 18.3 AviUtl2 側の保持方式

- SVG 文字列
- VectArtDesigner 独自シリアライズ
- 外部ファイル参照
- オブジェクト内埋め込み

##### 18.4 編集時

- AviUtl2 から編集ボタン
- VectArtDesigner GUI 表示
- 現在データを読み込み
- 編集
- 決定
- AviUtl2 へ反映
- キャンセル時は変更しない

##### 18.5 描画キャッシュ

動画では同一内容を多数フレーム描画するため必要。

- データ変更時のみ再生成
- サイズ変更時
- フレーム依存要素追加時
- GPU リソース管理

---

#### 19. 書き出し

候補：

- SVG
- PNG
- JPEG
- WebP
- MIF 最低限

検討項目：

- 背景透明
- 任意解像度
- 倍率指定
- アンチエイリアス
- 色空間

---

#### 20. キャンバス

- サイズ
- 背景色
- 透明背景
- 定型サイズ
- 1920x1080
- 1280x720
- サムネイル用プリセット
- 任意サイズ
- 表示倍率
- Fit
- 100%

---

#### 21. フォント

Text が重要なため早めに検討する。

- Windows フォント列挙
- フォント名
- フォントファイル参照
- SVG 保存時の扱い
- フォントが存在しない場合
- AviUtl2 側描画との一致
- Text → Path 化による互換確保

---

#### 22. パフォーマンス

想定する最大規模を決める。

- レイヤー数
- Path 頂点数
- 画像サイズ
- キャンバスサイズ
- Undo 履歴数

対策候補：

- レイヤー中間キャッシュ
- 選択対象周辺だけ再描画
- サムネイルキャッシュ
- 画像キャッシュ
- Path キャッシュ

---

#### 23. エラー・互換性表示

MIF / SVG 読み込みで重要。

検討項目：

- 非対応要素の一覧表示
- 読み込み失敗箇所
- 近似変換した要素
- 元ファイルを壊さない
- 読み込みログ

---

#### 24. 初期実装の境界

すべてを一度に実装しない。

最初の完成ラインを決める。

候補：

1. 基本データモデル
2. Rectangle / Line / Path
3. レイヤー
4. 選択・移動・サイズ変更
5. Fill / Stroke
6. SVG 保存・再読込
7. MIF 基本図形読込
8. Text
9. Image
10. Undo / Redo
11. AviUtl2 との最小連携

その後、サムネイル向け装飾や高度な Path 編集を追加する。

---

#### 25. Codex へ実装を渡す前に確定しておく項目

最低限、以下を決定してから本格実装へ進む。

- 内部オブジェクト一覧
- 共通プロパティ
- 座標系
- Transform 方式
- Group 構造
- Fill / Stroke 構造
- Text データ構造
- Image 保持方式
- Path データ構造
- SVG 対応範囲
- MIF → 内部形式変換表
- 保存形式
- Undo / Redo 方式
- 描画エンジンと GUI の分離境界
- AviUtl2 とのシリアライズ形式
- AviUtl2 側でのデータ保持方式
- 初期実装範囲

---

#### 26. 現時点での基本方針

現時点では次の考え方を基準とする。

- VectArtDesigner の内部形式を正本とする。
- MIF は読み込み重視の救済形式。
- MIF 書き出しは優先しない。
- SVG は完全互換を目指さず、対応サブセットのみ扱う。
- SVG の仕組みは描画・保存フレームワークとして利用する。
- サムネイル作成に必要な操作性・装飾機能を一般 SVG 互換性より優先する。
- 本体と AviUtl2 プラグインで可能な限り同一データ・同一描画エンジンを使用する。
- GUI に依存しない編集・描画コアを作る。
- Codex に個別機能を実装させる前に、データモデルと責務境界を確定する。
